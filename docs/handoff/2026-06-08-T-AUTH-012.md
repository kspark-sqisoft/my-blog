# Handoff — T-AUTH-012 (User.bio + posts 인덱스 + shared 타입) · author-profile 시작

날짜: 2026-06-08 / 기능: author-profile (E19) / 컨텍스트: Auth(+shared 토대)

## 한 일
- author-profile 의 스키마·타입 토대를 추가했다(ADR-0028, ADR-0025 amend).
  - `schema.prisma`: `User.bio String?`(소개) + `posts @@index([authorId, status, publishedAt])`(작성자별 발행글 조회). 마이그레이션 `20260608093026_author_profile_bio_index`(ADD COLUMN bio TEXT + CREATE INDEX, 비파괴), dev `blog` + test `blog_test` 적용, prisma generate.
  - shared: `AuthUserDto.bio` 추가, 신규 `AuthorProfileDto{id,name,avatarUrl,bio,createdAt,postCount}`(이메일 없음·공개), `UpdateProfileDto.bio?`(ADR-0025 amend), `PostSummaryDto.authorId`(작성자 링크). index export.
  - bio 노출 파급: `auth.service`(issue/updateProfile select+매핑), `jwt.strategy`(validate select+매핑)에 bio. `post.service.toSummary` 에 authorId 매핑.
  - mock 갱신: `optional-jwt-auth.guard.spec` + web 5 test(RoleRoute/AdminLayout/NavBar/CommentForm/Profile)에 bio:null.

## TDD/검증
- 토대(스키마·타입)라 동작 테스트는 후속(T-AUTH-013/014, T-PUB-107, T-WEB-402/403). 본 태스크는 **구조 + 회귀 0**.
- check_migration_destructive **false**, check_index **hasIndex true**(authorId 복합), psql 로 blog/blog_test 양쪽 bio·인덱스 확인(verifier).
- api tsc 0·단위 181 / web tsc·test 139 / api·web lint clean / shared build.
- 함정: AuthUserDto.bio 필수 추가 → 생성처(auth.service·jwt.strategy)·mock 8곳 갱신(tsc로 누락 추적). prisma generate 필수.

## 결정한 것
- 새 ADR 불필요(ADR-0028). bio "노출 매핑"까지가 012 범위 — bio "편집 로직"(updateProfile data.bio)·공개 프로필 API 는 T-AUTH-013/014.
- PostSummaryDto.authorId 는 toSummary 매핑까지 012 에서(컴파일 유지). ?author 필터는 T-PUB-107.

## 알려진 이슈
- `AuthorProfileDto` 는 현재 미사용 export(T-AUTH-013 이 소비) — `pnpm gc`(knip)가 일시적으로 잡을 수 있음(의도된 토대).

## 다음 세션이 알아야 할 것
- 다음 후보(병렬 가능, deps=012 done): **T-PUB-107**(listPublished author 필터 + ?author + e2e), **T-AUTH-013**(GET /api/users/:id 공개 프로필 + UserController), **T-AUTH-014**(PATCH me bio 편집). 그 뒤 T-WEB-402(프로필 페이지+링크, deps 107+013)·T-WEB-403(bio 편집 UI, deps 014).
- AuthUserDto.bio·toSummary authorId 이미 노출됨 — 후속은 편집 로직·공개 API·web 만.
- CLAUDE.md AUTO-MANAGED 표: GET /api/users/:id 는 T-AUTH-013 에서 추가.

## 명령 출력 증거
- 마이그레이션: ADD COLUMN bio + CREATE INDEX(비파괴), 양 DB up to date
- api tsc 0 / 단위 181 / web tsc 0 / web test 139 / lint clean
- verifier: 12/12 PASS / code-reviewer: Critical·Warning 0(Suggestion: AuthorProfileDto 후속 소비)
