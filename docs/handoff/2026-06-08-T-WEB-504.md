# Handoff — T-WEB-504 (작성자 시리즈 관리 UI) · series(E20) 완결

## 한 일
- 작성자/운영자용 시리즈 관리 화면 추가 (series, ADR-0029). 시리즈 CRUD + 멤버십/순서 관리를 web에서 조립. **series 기능의 마지막 태스크.**
- 신규/변경 파일:
  - `packages/web/src/admin/useSeriesAdmin.ts`(신규) — `useCreateSeries`/`useUpdateSeries(id)`/`useDeleteSeries`/`useSetSeriesPosts(id)` mutation. 성공 시 `['series-list']`·`['series',id]` 캐시 무효화.
  - `packages/web/src/pages/admin/SeriesManage.tsx`(신규) — `/admin/series`: `useSeriesList(pageSize 100)` → 역할/작성자 클라이언트 필터(AUTHOR 본인·ADMIN 전체) + 생성 폼(react-hook-form+zod, title 1~120) → POST → 편집 화면 navigate, 행별 수정 링크·삭제.
  - `packages/web/src/pages/admin/SeriesEditor.tsx`(신규) — `/admin/series/:id/edit`: `useSeries(id)` 로드 → 제목·설명 폼(인라인 zod safeParse, title 1~120·desc 0~500) PATCH, `usePosts({author})` 후보(본인 글 필터) + `orderedIds` 상태(추가/제거/↑↓) → `PUT /series/:id/posts`, 삭제 후 목록 navigate.
  - `packages/web/src/routes.tsx` — `/admin/series`·`/admin/series/:id/edit`(RoleRoute AUTHOR/ADMIN 하위).
  - 테스트: `SeriesManage.test.tsx`(5), `SeriesEditor.test.tsx`(6).

## TDD 사이클 확인
- **RED**: SeriesManage.test(5)·SeriesEditor.test(6) → 컴포넌트 부재로 실패.
- **GREEN**: useSeriesAdmin → SeriesManage → SeriesEditor → routes → 11건 통과.
- **REFACTOR(보강)**: code-reviewer Warning 반영 — ① 후보 글을 `p.authorId === user.id` 로 명시 필터(user 미로드 시 타인 글 노출 방지) ② 순서 이동(↑↓) 후 PUT 순서 반영 테스트 추가.

## 결정한 것
- "내 시리즈 목록"은 전용 API 없이 **공개 `GET /api/series`(pageSize 100) + 클라이언트 역할/작성자 필터**로 구현(MVP, 100개 상한 — 후속 페이지네이션 여지). 쓰기 권한은 서버 Actor가 강제(클라 필터는 표시용).
- 폼 검증은 web 인라인 zod(생성=RHF+zodResolver, 편집=safeParse), 서버 class-validator(title 1~120·desc 0~500·postIds≤100)와 정합. shared에 zod 미추가(함정 #9).
- SeriesEditor 폼 초기화는 PostEditor의 렌더타임 가드(loadedId) 패턴 재사용(무한루프 없음).
- `useSeriesAdmin` 위치는 `src/admin/`(admin 훅 관행). tasks 원안 `src/series/`에서 조정 — .md에 메모.

## 알려진 이슈
- 없음. web 174/174 GREEN, typecheck·lint 클린.
- code-reviewer Warning(미반영, 비차단): ① 삭제 confirm 없음(시리즈 삭제는 SetNull로 글 보존돼 저위험) ② 빈 설명 `''` 전송(bio와 동일 — 공개 렌더 `description &&`로 미표시 일관) ③ ADMIN의 타인 시리즈 편집 시 후보는 ADMIN 본인 글만(서버는 ADMIN 임의 허용 — UI 한계). 후속 개선 여지.

## 다음 세션이 알아야 할 것
- 🎉 **series(E20) 기능 완결**: T-SER-001~005(백엔드) + T-WEB-501~504(web) 전부 done. `feature_list.json` E20 9개 항목 모두 done.
- 6-feature 로드맵 진행: seo-feed·comment-moderation·author-profile·**series 완료**. **남은 2개: bookmarks(ADR-0030), notifications(ADR-0031)** — 각각 새 기능이라 10-Phase(PRD→BC→TRD→ADR→tasks→TDD)로 시작.
- 다음 기능 `bookmarks`: Engagement 확장(Like 복합PK 패턴), 개인 저장(좋아요와 별개).
- 미push 커밋들 존재 가능 — 다음 세션 시작 시 `git push` 확인.

## 명령 출력 증거
- 단위: SeriesManage 5 + SeriesEditor 6 GREEN. 전체 `pnpm --filter web test` → **37 files / 174 tests passed**.
- typecheck: `tsc -b --noEmit` 통과. lint: `eslint .` 통과.
- verifier: acceptance 3/3 **PASS**(목록·생성·편집·순서 PUT·삭제·zod·훅경유·권한). code-reviewer: Critical 0 / Warning 4(반영 2: 후보 본인글 필터·순서이동 테스트 / 선택 2: confirm·빈설명) / Suggestion(렌더초기화 안전 확인).

평가: Accept (12/12)
