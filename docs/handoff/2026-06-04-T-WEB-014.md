# Handoff — T-WEB-014 사용자 관리 화면 /admin/users (ADMIN)

날짜: 2026-06-04 / 기능: multiuser-rbac (E9, WEB)

## 한 일
- `admin/useAdminUsers.ts` 신규: `useAdminUsers`(GET /admin/users, Paginated) +
  `useUpdateUserRole`(PATCH /admin/users/:id/role, 성공 시 ['admin-users'] 무효화).
- `pages/admin/UserManagement.tsx` 신규: 이름·이메일·역할 셀렉트(ADMIN/AUTHOR/MEMBER)·가입일
  테이블. 셀렉트 변경 시 즉시 역할 변경. 409→"마지막 관리자…" 에러, 그 외→일반 실패 메시지.
- `components/layout/AdminLayout.tsx`: `user.role === 'ADMIN'` 일 때만 "사용자 관리" 사이드바 링크.
- `routes.tsx`: 운영자 셸 내부에 `/admin/users` 추가, `<RoleRoute roles={['ADMIN']}>` 로
  한 번 더 게이팅(AUTHOR 접근 시 / 리다이렉트). routes.test 인벤토리에도 등록.

## TDD 사이클 확인
- RED: `UserManagement.test`(모듈 미존재)·`AdminLayout.test`(사용자 관리 링크 없음) → 2 files failed.
- GREEN: 훅 + 페이지 + 사이드바 링크 + 라우트 구현 후 50 passed (18 files).
- REFACTOR: 불필요(useAdminPosts 패턴 그대로 미러링).

## 결정한 것
- 응답 본문은 axios `res.data` 가 곧 `Paginated`(기존 useAdminPosts 와 동일 계약). 신규 ADR 불필요.
- `/admin/users` 는 운영자 셸(AdminLayout) 안에 두되 `RoleRoute roles={['ADMIN']}` 로
  중첩 게이팅 — AUTHOR 도 셸은 보지만 이 라우트만 ADMIN 전용.
- 역할 변경은 낙관적 갱신 없이 mutation→invalidate 방식(실패 시 기존 값 유지, 단순/안전).

## 알려진 이슈
- e2e(Playwright)는 라이브 서버 필요라 미실행. acceptance 는 Vitest 로 전부 커버.
- 페이지네이션은 page=1,pageSize=50 고정(목록형 최소 구현, 기존 Dashboard 와 동일 관행).

## 다음 세션이 알아야 할 것
- 다음 후보: **T-WEB-015**(댓글 폼 실명 자동 + CommentTree authorName).
  deps T-CONV-004·T-WEB-012 모두 done. 로그인 시 이름 입력 숨기고 user.name 표시,
  displayName 미전송; CommentTree 는 authorName 우선 표시; 가입→승격→작성 GWT e2e.
- E9 마지막 태스크. 완료 시 multiuser-rbac 기능 전체 종료.

## 명령 출력 증거
- lint: eslint . → 통과(에러 0)
- typecheck: tsc -b --noEmit → 통과(에러 0)
- test: Test Files 18 passed / Tests 50 passed
