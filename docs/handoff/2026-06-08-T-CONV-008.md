# Handoff — T-CONV-008 (CommentController PATCH/DELETE + 가드 + e2e)

날짜: 2026-06-08 / 기능: comment-moderation (E18) / 컨텍스트: Conversation

## 한 일
- 댓글 수정·삭제 엔드포인트를 TDD 로 배선했다(ADR-0027).
  - `comment.controller.ts`: `@Patch(':id')`·`@Delete(':id')` + `@UseGuards(JwtAuthGuard, ThrottlerGuard)`. `actorOf(req)` 로 `req.user{id,role}`(JwtStrategy DB 재조회) 전달. DELETE `@HttpCode(204)`.
  - `dto/update-comment.dto.ts`: `UpdateCommentDto` class-validator(body `@MinLength(1)`·`@MaxLength(COMMENT_BODY_MAX)`).
  - `dto/create-comment.dto.ts`: `BODY_MAX` → `export const COMMENT_BODY_MAX`(상수 재사용).
  - `test/comment-moderation.e2e-spec.ts`: 권한 매트릭스 8 케이스 — **별도 spec 으로 분리**(같은 spec 의 429 테스트가 throttler 60s/10회를 소진해 후속 요청을 막기 때문 → 독립 앱=독립 카운터).
- CLAUDE.md AUTO-MANAGED 표에 PATCH/DELETE comments 추가.

## TDD 사이클
- **RED**: PATCH/DELETE 라우트 미구현(comment.e2e 에 추가 시) → 모더레이션 케이스 실패.
- **함정(throttler)**: 모더레이션을 기존 comment.e2e 에 넣으니 "soft 부모 답글 POST → 400"이 **429** — 같은 spec 의 429 테스트(30 POST)가 throttler(60s/10회) 소진. → comment.e2e 원복(`git checkout`) + 모더레이션을 **별도 spec** 으로 분리.
- **GREEN**: comment-moderation.e2e 8 통과. 전체 e2e 99, 단위 181.

## 결정한 것
- 새 ADR 불필요(ADR-0027). 가드 순서 `JwtAuthGuard → ThrottlerGuard`(미인증은 throttler 카운트 전 401). 권한 판정은 service(컨트롤러는 actor 전달만).
- e2e 권한 매트릭스를 throttler 독립 위해 별도 spec 으로 분리(429 테스트와 공존 불가).

## 알려진 이슈 / 후속
- e2e 의 "본인/ADMIN/글쓴이 204"는 시드 author 가 세 권한을 한 신원으로 동시 충족 — ADMIN-only·글쓴이-only 분리 경로는 단위 `comment.service.spec`(T-CONV-007)이 커버. (code-reviewer/verifier 주의, 단위 교차 커버로 충분)
- (T-CONV-007 이월) `Comment.parentId` 인덱스 부재 — 후속 마이그레이션 백로그.

## 다음 세션이 알아야 할 것
- 다음: **T-WEB-401** (웹 댓글 수정·삭제 UI + 훅, deps=008·005 done). `src/api/comments.ts` 에 `useUpdateComment`·`useDeleteComment`(목록 무효화), CommentTree/CommentItem 에 권한별 버튼(`canEdit=본인`, `canDelete=본인·ADMIN·글쓴이(postAuthorId prop)`), 수정 인라인 폼·"수정됨", 삭제 확인·"삭제된 댓글입니다". PostDetail 의 `authorId` 를 트리에 prop. e2e GWT.
- **comment-moderation API 완성**: 수정/삭제/소프트삭제/모더레이션 권한 모두 동작. 남은 건 웹 UI(T-WEB-401)뿐.

## 명령 출력 증거
- comment-moderation e2e: 8 passed(가림 4종·soft/hard 왕복·401/403/404 매트릭스)
- 전체: 단위 181 / e2e 99(14 suites) / tsc 0 / lint clean
- verifier: 전 acceptance PASS(익명 수정403·익명 운영자 삭제204·타인 삭제403은 단위 교차 커버) / code-reviewer: Critical·Warning 0
