# Handoff — T-AUTH-004 (AuthController: login/logout/me)

- 날짜: 2026-06-02
- 태스크: T-AUTH-004
- Context: AUTH
- status: done
- 의존: T-AUTH-003 (done)
- 관련: ADR-0001

## 한 일

- `src/auth/dto/login.dto.ts` — `LoginDto { email(@IsEmail), password(@IsString) }`.
- `src/auth/auth.controller.ts`:
  - `POST /api/auth/login` (@HttpCode 200): AuthService.login → `res.cookie('access_token', token, { httpOnly, sameSite:'lax', secure(prod), maxAge:1h, path:'/' })`, `{ user }` 반환.
  - `POST /api/auth/logout` (@HttpCode 200): `res.clearCookie` → `{ success: true }`.
  - `GET /api/auth/me` (@UseGuards JwtAuthGuard): `{ user: req.user }`.
- `src/auth/auth.module.ts`: PassportModule + JwtModule.registerAsync(JWT_SECRET) + AuthService + JwtStrategy + AuthController.
- `app.module.ts`에 AuthModule 연결.
- e2e `test/auth.e2e-spec.ts` 5케이스.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | login 200 + HttpOnly/SameSite 쿠키 + user | ✅ |
| 2 | logout 쿠키 만료 | ✅ |
| 3 | me 유효쿠키 200 / 비로그인 401 | ✅ |

게이트: lint 0, tsc 0, unit 10/10, e2e 14/14.

## E1(인증) 완료

T-AUTH-001~004 모두 done. 운영자 인증 흐름(시드 → 자격검증 → JWT 쿠키 → 가드 → login/logout/me) 완비.

## 다음 태스크가 알아야 할 것

- **쓰기 엔드포인트 보호**: `@UseGuards(JwtAuthGuard)`만 붙이면 됨(AuthModule이 JwtStrategy 제공, PassportModule 전역 아님 → 다른 모듈에서 JwtAuthGuard 쓰려면 해당 모듈이 PassportModule/전략에 접근 가능해야 함).
  - 주의: JwtStrategy는 AuthModule에서만 provider로 등록됨. E2/E4(Post/Comment) 컨트롤러에서 JwtAuthGuard 사용 시, AuthModule을 import 하거나 JwtStrategy를 전역 등록해야 한다. → **T-PUB-006 착수 시 AuthModule export 또는 글로벌 전략 등록 검토 필요**.
- `req.user`는 `{ id, email }`(AuthUserDto). Post 작성 시 authorId = req.user.id.
- JWT_SECRET 운영 필수.

## 변경 파일

- `packages/api/src/auth/dto/login.dto.ts` (신규)
- `packages/api/src/auth/auth.controller.ts` (신규)
- `packages/api/src/auth/auth.module.ts` (신규)
- `packages/api/src/app.module.ts` (AuthModule import)
- `packages/api/test/auth.e2e-spec.ts` (신규)
- `docs/tasks/blog-mvp.md` (status done)
