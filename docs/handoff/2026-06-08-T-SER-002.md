# Handoff — T-SER-002 (SeriesService/Controller 생성·수정·삭제 + 권한)

## 한 일
- 시리즈 CRUD API 추가 (series, ADR-0029). `POST/PATCH/DELETE /api/series` + Actor 소유권(ADR-0018 재사용).
- 신규/변경 파일:
  - `packages/api/src/publishing/series.service.ts`(신규) — `create`(authorId=actor, slug 파생) / `update`(소유자·ADMIN, slug 불변) / `remove`(소유자·ADMIN, SetNull 보존) / `generateUniqueSlug`(series, ADR-0022 동형) / `assertCanMutate`(미존재 404·비소유 403) / `toDetail`(발행글 요약 매핑+postCount).
  - `packages/api/src/publishing/series.controller.ts`(신규) — `@Controller('series')` POST/PATCH/DELETE, `JwtAuthGuard + RolesGuard('AUTHOR','ADMIN')`, `actorOf`.
  - `packages/api/src/publishing/dto/create-series.dto.ts`·`update-series.dto.ts`(신규) — class-validator(title 1~120, description nullable 0~500).
  - `packages/api/src/publishing/publishing.module.ts` — Series 등록·export.
  - `packages/shared/src/dto/series.ts` — `SeriesPostItemDto` 명명 타입 추출(인라인 인터섹션 → named, T-SER-001 리뷰 Suggestion 반영) + index export.
  - 테스트: `series.service.spec.ts`(통합 8), `test/series.e2e-spec.ts`(e2e 5).
- `CLAUDE.md` AUTO-MANAGED 표에 시리즈 쓰기 3엔드포인트 추가(공개 GET 은 T-SER-004 후).

## TDD 사이클 확인
- **RED**: series.service.spec(8) + series.e2e-spec(5) → SeriesService 부재로 컴파일 실패.
- **GREEN**: api DTO → SeriesService → SeriesController → 모듈 등록 → 단위 8·e2e 5 통과.
- **REFACTOR**: `SeriesPostItemDto` 명명 추출(T-SER-001 Suggestion). e2e 미사용 import(`seedOperator`) 제거(lint).

## 결정한 것 (ADR-0029 기반, 새 ADR 불필요)
- 소유권: `assertCanMutate` 가 `actor.role==='ADMIN' || series.authorId===actor.id` 아니면 403, 미존재 404. update·remove 둘 다 mutate 전 호출.
- slug 불변(update data 에 slug 미포함) — 링크 안정성(ADR-0029 #4).
- `toDetail` 은 발행글만(`status=PUBLISHED`) `seriesOrder` 정렬로 매핑(발행 격리). 멤버십이 없는 현 단계에선 posts=[]·postCount=0. **실제 멤버십은 T-SER-003, 공개 목록·상세는 T-SER-004** 가 채운다.
- 요약 매핑은 기존 `toSummaryText`·`extractFirstImageUrl` 재사용(중복 로직 회피).

## 알려진 이슈
- 없음. api 단위 198·e2e 113 GREEN, lint·tsc 클린. DB 가드(scan_pii_logging 0건, slug unique·`(seriesId,seriesOrder)` 인덱스 존재).
- code-reviewer Suggestion(선택, 미반영): ① `assertCanMutate` 후 update/delete 2회 쿼리(403/404 구분 위해 현 분기 유지 권장) ② `generateUniqueSlug` race(최종 방어선 slug @unique, 기존 Post slug 동형 한계) ③ DTO `@ValidateIf` 조합(동작 정확).

## 다음 세션이 알아야 할 것
- 다음 후보: **T-SER-003**(`PUT /api/series/:id/posts` 멤버십·순서 원자 재지정, deps T-SER-001·002 done). AUTHOR 본인 글만/ADMIN 임의, postIds≤100, 트랜잭션 롤백.
- 이어서 T-SER-004(공개 목록·상세 — `toDetail` posts 채움이 여기서 실효), T-SER-005(글 상세 네비), web T-WEB-501~504.
- `SeriesService.toDetail` 은 이미 발행글 매핑을 하므로 T-SER-004 의 상세는 이 메서드를 재사용/확장.

## 명령 출력 증거
- 단위: `pnpm --filter api test -- series.service.spec` → **8 passed**. 전체 `pnpm --filter api test` → **198 passed / 28 suites**.
- e2e: `pnpm --filter api test:e2e -- series.e2e-spec` → **5 passed**. 전체 `pnpm --filter api test:e2e` → **113 passed / 16 suites**.
- lint: `eslint --fix` 통과(초기 unused import 1 → 제거 후 0). typecheck: `tsc --noEmit` EXIT 0.
- verifier: acceptance 4/4 **PASS**(세부 10건). code-reviewer: Critical 0 / Warning 0 / Suggestion 3(선택). DB 가드: scan_pii_logging 0, check_index 통과.

평가: Accept (12/12)
