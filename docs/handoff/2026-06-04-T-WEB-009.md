# Handoff — T-WEB-009 (애플 스타일 디자인 시스템 + 레이아웃 + 테마)

- 날짜: 2026-06-04
- 태스크: T-WEB-009
- Context: WEB
- status: done
- 의존: T-WEB-007, T-WEB-008 (done)
- 기능: apple-redesign (PRD/TRD/tasks 동명 문서)

## 한 일

claude.ai/design의 애플 시안("디버그 노트")을 `packages/web`에 이식.

- `src/index.css` — 시맨틱 CSS 토큰(라이트 + `[data-theme='dark']`) + `ab-*` 컴포넌트 클래스 전면 정의. `@import 'tailwindcss'` 유지. `#root` 고정폭/보더 제거.
- `index.html` — `lang="ko"`, Pretendard CDN `<link>`, title "디버그 노트".
- `src/theme/useTheme.ts` — zustand 테마 스토어(토글·localStorage `blog-theme`·`[data-theme]` 동기화).
- 공통 크롬: `components/{NavBar,Footer,Icon}.tsx`, `components/layout/{PublicLayout,AdminLayout}.tsx`, `lib/{site,format}.ts`.
- `routes.tsx` — 공개(PublicLayout)/운영자(ProtectedRoute+AdminLayout)/로그인(단독) 중첩 라우트.
- 페이지/컴포넌트 리스타일: Home/PostDetail/TagPosts/Login/Dashboard/PostEditor/Comments/Pagination/Markdown.

## TDD 사이클 확인

- **RED**: `src/theme/useTheme.test.ts` 작성 → 모듈 부재로 import 실패(1 failed, 0 tests).
- **GREEN**: `useTheme.ts` 구현 → 2 passed (토글 light↔dark + `[data-theme]`/localStorage 반영).
- **시각 리스타일**: 신규 로직이 아니라 className 교체 위주 → 기존 단위 테스트(역할/라벨/동작)와 E2E 셀렉터가 회귀 가드. 표시 형식 변경 2건만 테스트 갱신(날짜 한국어 표기, 라우트 등록 검증을 중첩 경로까지 재귀 수집).

## 결정한 것

- **ADR-0016 (Proposed)** — 디자인 시스템을 CSS 변수 토큰 + `data-theme` 수동 토글로 구성. 사용자 승인 대기.
- 실제 데이터에 없는 필드(author/category/readingTime/cover)는 **창작하지 않음** — 실존 필드(제목/요약/태그/날짜)로 매거진 감성 재현. 브랜드명만 시안의 "디버그 노트" 채택.
- 접근성 이름·요소 타입(`ul/li`, `a`, `.prose`)·동작 보존(대시보드 즉시 삭제 유지, 확인 모달 미도입).

## 알려진 이슈

- 전역 `index.css` 비대(토큰+컴포넌트 클래스). Tailwind 유틸과 `ab-*` 혼재(ADR-0016 트레이드오프).
- 번들 경고(>500kB) — 기존부터 있던 코드 스플리팅 미적용 이슈(범위 외).

## 다음 세션이 알아야 할 것

- 테마는 `[data-theme]` 속성 + localStorage `blog-theme`. 새 색은 토큰으로만 추가.
- 목록 대표 이미지는 후속 T-PUB-103/T-WEB-010에서 처리.

## 명령 출력 증거

```
typecheck: tsc -b --noEmit → 0
unit: Test Files 13 passed (13), Tests 34 passed (34)
lint: eslint . → 0
build: vite build → ✓ built
브라우저 실측: 라이트/다크 홈 + 로그인 카드 렌더, 토글 시 body bg rgb(0,0,0)·localStorage=dark 확인
```

commit: `8969b46 feat(web): 애플 스타일 디자인 시스템 적용 (디버그 노트 시안)`
