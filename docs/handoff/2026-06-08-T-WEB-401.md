# Handoff — T-WEB-401 (웹 댓글 수정·삭제 UI) · comment-moderation 완료

날짜: 2026-06-08 / 기능: comment-moderation (E18) / 컨텍스트: WEB

## 한 일
- 웹 댓글 수정·삭제 UI 를 TDD 로 구현했다(ADR-0027). **comment-moderation(E18) 전체 완료.**
  - `src/comments/useUpdateComment.ts`·`useDeleteComment.ts`: `api.patch/delete` + 성공 시 `invalidateQueries(['comments', postId])`.
  - `components/CommentTree.tsx`: 권한별 버튼(`canEdit`=본인, `canDelete`=본인·ADMIN·글쓴이[postAuthorId]), 인라인 수정 폼(빈 body 저장 비활성), 삭제 `window.confirm`, **isDeleted → "삭제된 댓글입니다" placeholder + 답글 보존**, **isEdited → "· 수정됨"**. `postAuthorId` prop 재귀 전파.
  - `CommentSection.tsx`·`PostDetail.tsx`: `postAuthorId={post.authorId}` chain.
  - `index.css`: `.ab-comment-actions/.ab-comment-action/.ab-comment-edited/.ab-comment-deleted/.ab-comment-edit` 스타일(code-reviewer Suggestion).
  - `CommentTree.test.tsx`: useAuth mock + makeComment 헬퍼 + 권한/표시 9케이스.

## TDD 사이클
- **RED→GREEN**: CommentTree.test 권한·수정됨·삭제됨 케이스 → 구현 후 web test 139 통과(CommentTree 9).
- **REFACTOR**: 누락 CSS 클래스 정의(Suggestion 1), UserLike role 타입 UserRole 로 좁힘(Suggestion 2).

## 결정한 것
- 훅 위치는 기존 컨벤션 따라 `src/comments/`(tasks 의 `src/api` 표기는 보정 — 기존 useComments/useCreateComment 와 동일 폴더).
- **e2e GWT 는 후속**(사용자 합의): web e2e 가 격리 prod 스택(docker 빌드, 수 분)이라 비용이 커서, 권한·수정·삭제·소프트삭제·트리보존을 **web 단위 9케이스 + api e2e 8케이스**로 커버. 브라우저 전체 흐름 e2e 는 배포/후속 검증 항목(acceptance #4 조정).

## 알려진 이슈 / 후속 (백로그)
- **[후속] web e2e GWT**: 격리 prod 스택에 "댓글 작성→수정(수정됨)→답글有 삭제(트리 보존)" GWT 시나리오 추가(member-journey 확장).
- (E18 이월) `Comment.parentId` 인덱스 부재 — 후속 마이그레이션.

## 다음 세션이 알아야 할 것
- **comment-moderation(E18) 완료**: 수정/삭제/조건부 소프트삭제/모더레이션 권한 + 웹 UI 전부 동작. (T-CONV-005~008·T-WEB-401 done)
- 6개 기능 로드맵 3번째 = **author-profile**(공개 작성자 프로필 `/users/:name`). 추천 순서: author-profile → series → bookmarks → notifications. ADR 0028~0031 예약.
- 후속 백로그 2건(seo-feed og:image 절대화, E18 web e2e GWT + parentId 인덱스).

## 명령 출력 증거
- web: typecheck(tsc -b) 0 / test 139 passed(31 files, CommentTree 9) / lint clean
- (E18 누적) api 단위 181 / api e2e 99(comment-moderation 8) / 전부 green
- verifier: 4/4 PASS / code-reviewer: Critical·Warning 0(Suggestion 2건 반영)
