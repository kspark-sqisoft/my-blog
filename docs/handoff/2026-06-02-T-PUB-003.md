# Handoff — T-PUB-003 (TagService: 0~5 검증 + PostTag 연결)

- 날짜: 2026-06-02
- 태스크: T-PUB-003
- Context: PUBLISHING
- status: done
- 의존: T-PUB-001 (done)
- 관련: ADR-0006

## 한 일

- `src/publishing/tag.service.ts` — `TagService`:
  - `assertWithinLimit(tags)` — MAX_TAGS=5 초과 시 BadRequestException.
  - `createInput(tags)` / `replaceInput(tags)` — connectOrCreate 중첩 입력(생성용/교체용).
- **PostService 리팩터**: 인라인 `tagCreate` 제거 → `TagService` 주입.
  - create/update에서 `assertWithinLimit` + `createInput`/`replaceInput` 위임.
  - → 0~5 검증이 PostService(컨트롤러) 경로에도 적용됨.
- `post.service.spec.ts` 모듈 설정에 `TagService` provider 추가(DI).

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | 0~5 연결 + 동일 name 재사용 | ✅ connectOrCreate, tag count=1 |
| 2 | 6개 이상 BadRequest | ✅ TagService 직접 + PostService 경로 |
| 3 | 수정 시 집합 교체 | ✅ deleteMany+create |

게이트: lint 0, tsc 0, unit 25/25, e2e 14/14 (PostService 리팩터 회귀 없음).

## 다음 태스크가 알아야 할 것

- **T-PUB-007 (GET /api/tags)**: 발행 Post에 쓰인 Tag + postCount 집계. TagService에 조회 메서드 추가 예정.
- 컨트롤러(T-PUB-006)는 PostService만 호출하면 tag 검증이 자동 적용됨(별도 처리 불필요).
- Tag는 독립 Aggregate 아님 — Post를 통해서만 다룬다(생명주기 PostService 경유).

## 변경 파일

- `packages/api/src/publishing/tag.service.ts` (신규)
- `packages/api/src/publishing/tag.service.spec.ts` (신규)
- `packages/api/src/publishing/post.service.ts` (TagService 위임)
- `packages/api/src/publishing/post.service.spec.ts` (TagService provider)
- `docs/tasks/blog-mvp.md` (status done)
