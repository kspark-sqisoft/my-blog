# Handoff — T-SEO-001 (seo-feed 절대 URL·XML 이스케이프 유틸 + 환경변수)

날짜: 2026-06-08 / 기능: seo-feed (E17) / 컨텍스트: Publishing

## 한 일
- seo-feed 의 산출물 생성 기반 유틸을 TDD 로 추가했다(ADR-0026).
  - `packages/api/src/publishing/seo/site-url.ts`: `siteUrl()`(SITE_URL, dev 기본값 `http://localhost:5173`, 후행 슬래시 제거), `absoluteUrl(pathOrUrl, base?)`(상대→절대, 한글 슬러그 세그먼트별 인코딩, 이미 절대면 그대로), `isExternalUrl(url)`(외부 http(s) 판별 — og:image 폴백 판정용).
  - `packages/api/src/publishing/seo/xml.ts`: `escapeXml(s)`(`& < > " '` 치환, `&` 우선으로 이중 이스케이프 방지 — 피드·OG 메타 주입 방지).
- 환경변수 추가: `.env.example` 과 `docker-compose.yml`(api environment) 에 `SITE_URL`·`FEED_MAX_ITEMS=20`.
- 선재 타입 에러 핫픽스: `optional-jwt-auth.guard.spec.ts` 의 mock user 에 `avatarUrl: null` 추가(profile-avatar 머지 시 누락된 `AuthUserDto.avatarUrl` required 불일치 — T-SEO-001 무관 잠복 버그, tsc 게이트 차단 해소).

## TDD 사이클 확인
- **RED**: `pnpm --filter api test -- src/publishing/seo` → `Cannot find module './xml'`/`'./site-url'` (2 suites failed).
- **GREEN**: 유틸 구현 후 동일 명령 → `Test Suites: 2 passed, Tests: 11 passed`(site-url 7 + xml 4).
- **REFACTOR**: `encodePath` 세그먼트 인코딩 분리, 상수 추출(`DEV_DEFAULT_SITE_URL`·`ABSOLUTE_URL_RE`), absoluteUrl 입력 계약 주석 추가. 재실행 GREEN 유지.

## 결정한 것
- 새 ADR 불필요(ADR-0026 에서 이미 결정). 본 태스크는 그 결정의 기반 유틸 구현.
- `isExternalUrl` 은 현재 `^https?://` 만 외부로 본다. **T-SEO-004(og:image 폴백)** 에서 "`/uploads` 로 시작하는 것만 내부로 신뢰"하는 화이트리스트로 재확인할 것(code-reviewer Suggestion 2 — protocol-relative `//host` 우회 방지).
- `absoluteUrl` 입력 계약: **디코딩된 원본 경로**(DB 원본 slug)만 넘긴다(이중 인코딩 방지, 주석 명시).

## 알려진 이슈
- 없음. lint/tsc/단위(154) 전부 green.
- docker-compose.yml 변경으로 dev 컨테이너 재빌드 펜딩(`.claude/.docker-dirty.json`). SITE_URL 런타임 반영은 **T-SEO-002(/feed.xml 라이브 검증)** 시작 시 재빌드로 처리: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build`.

## 다음 세션이 알아야 할 것
- 다음 태스크: **T-SEO-002** (FeedService + `GET /feed.xml`, deps=T-SEO-001 done). `absoluteUrl`/`escapeXml`·`toSummaryText`(기존) 재사용, `setGlobalPrefix` exclude 로 루트 경로 노출, e2e 왕복(200 + application/rss+xml, 초안 제외, N+1 없음).
- seo 산출물은 api 전용, shared 무변경(함정 #9 안전) 유지.

## 명령 출력 증거
- lint: clean(eslint --fix, 출력 없음)
- tsc: `--noEmit` error 0
- 단위: `Test Suites: 22 passed, Tests: 154 passed`
- verifier: acceptance 7단언 전부 PASS / code-reviewer: Critical·Warning 0(Suggestion 2 — 후속 반영)
