# Handoff — T-PUB-101 (StorageProvider + LocalStorageProvider)

- 날짜: 2026-06-02
- 태스크: T-PUB-101
- Context: PUBLISHING (Upload)
- status: done
- 의존: T-INFRA-002 (done)
- 관련: ADR-0012

## 한 일

- `storage/storage.provider.ts` — 추상 `StorageProvider`(`save(StoredFile): Promise<SavedFile>`) + 타입.
  (Nest DI 토큰 겸용 — `{ provide: StorageProvider, useClass: ... }`)
- `storage/local-storage.provider.ts` — `LocalStorageProvider`:
  - `randomBytes(16)` 기반 추측 불가능 파일명 + 안전한 확장자(영숫자만, 없으면 MIME 폴백).
  - `UPLOAD_DIR`/`UPLOAD_URL_BASE` 환경변수 주입.
  - originalName은 확장자만 사용 → 경로 traversal 구조적 차단 + 베이스 디렉터리 밖 거부 안전장치.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | 추측 불가능 파일명 저장 + 접근 URL | ✅ |
| 2 | 경로/URL 베이스 환경변수 주입 | ✅ UPLOAD_DIR/UPLOAD_URL_BASE |
| 3 | traversal 차단 | ✅ 디렉터리 밖 탈출 불가 |

게이트: lint 0, tsc 0, unit 35/35 (storage 4).

## 다음 태스크가 알아야 할 것 (T-PUB-102)

- `POST /api/uploads`(운영자, JwtAuthGuard) + multer(@nestjs/platform-express) 메모리 스토리지.
- MIME 화이트리스트(image/*) + 크기 상한 검증 → 400/413.
- `StorageProvider.save({ buffer, originalName, mimeType })` 호출 → `{ url, contentType, size }` 반환(UploadResultDto).
- PublishingModule provider에 `{ provide: StorageProvider, useClass: LocalStorageProvider }` 등록.
- 운영 정적 서빙: nginx `/uploads/` (이미 nginx.conf SPA 폴백과 함께; UPLOAD_URL_BASE=/uploads 정합). 환경변수는 `.env.example`에 추가 권장.

## 변경 파일

- `packages/api/src/publishing/storage/storage.provider.ts` (신규)
- `packages/api/src/publishing/storage/local-storage.provider.ts` (신규)
- `packages/api/src/publishing/storage/local-storage.provider.spec.ts` (신규)
- `docs/tasks/blog-mvp.md` (status done)
