# Handoff — T-WEB-006 (운영자 대시보드 + 발행/삭제)

- 날짜: 2026-06-02
- 태스크: T-WEB-006
- Context: WEB
- status: done
- 의존: T-WEB-002, T-PUB-006, T-PUB-008 (done)

## 한 일

- `src/admin/useAdminPosts.ts` — `useAdminPosts`(GET /api/admin/posts) + `useAdminPostActions`(publish/unpublish/remove, 성공 시 `['admin-posts']` 무효화).
- `src/pages/admin/Dashboard.tsx` — 초안+발행 목록, status 배지, 행별 수정 링크 + 발행/발행취소/삭제 버튼.
- `routes.tsx`: `/admin` → `<ProtectedRoute><Dashboard/></ProtectedRoute>`.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | 초안+발행 함께(보호 라우트) | ✅ |
| 2 | 발행/취소/삭제 API + 목록 갱신 | ✅ mutation invalidate |
| 3 | 비인증 → 로그인 | ✅ ProtectedRoute(T-WEB-002 검증) |

게이트: lint 0, typecheck 0, test 28/28, build green.

## 다음 태스크가 알아야 할 것 (T-WEB-007)

- 작성/수정 에디터: `/admin/posts/new`, `/admin/posts/:id/edit` 자리표시자 교체.
- 생성 `POST /api/posts`, 수정 `PATCH /api/posts/:id`. 수정 시 기존 값 로드는 운영자용 상세가 필요 —
  현재 공개 `GET /posts/:id` 는 발행글만(초안 404). **초안 편집 로드를 위해 admin 상세 또는 목록의 항목 활용 필요**(검토).
- 이미지 업로드 `POST /api/uploads`(multipart) → 반환 URL 을 본문 마크다운에 삽입.
- Tag 6개 이상 클라 검증.

## 변경 파일

- `packages/web/src/admin/useAdminPosts.ts` (신규)
- `packages/web/src/pages/admin/Dashboard.tsx` (+ test, 신규)
- `packages/web/src/routes.tsx`
- `feature_list.json`, `docs/tasks/blog-mvp.md` (status done)
