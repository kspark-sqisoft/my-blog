# Handoff — T-CONV-002 (Comment 목록 조회: 깊이 2 중첩 직렬화)

- 날짜: 2026-06-02
- 태스크: T-CONV-002
- Context: CONVERSATION
- status: done
- 의존: T-CONV-001 (done)
- 관련: ADR-0013

## 한 일

- `CommentService.listByPost(postId)` → `CommentDto[]`:
  - `createdAt asc`로 조회 → 부모가 항상 자식보다 먼저 등장.
  - id→DTO 맵 빌드 후, parentId로 트리 구성. depth = parent.depth + 1.
  - 최상위(parentId null)만 roots로 반환, 답글은 부모 replies에 작성순 push.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | 작성순 depth 0→1→2 중첩 | ✅ |
| 2 | 각 노드 depth 필드 | ✅ |
| 3 | 답글 없는 노드 replies 빈 배열 | ✅ |

게이트: lint 0, tsc 0, unit 41/41, e2e 24/24.

## 다음 태스크가 알아야 할 것 (T-CONV-003)

- `CommentController`(ConversationModule):
  - `GET /api/posts/:postId/comments`(공개) → listByPost.
  - `POST /api/posts/:postId/comments`(공개, 익명) → create. body·displayName·parentId DTO.
- `@nestjs/throttler` 추가 → POST 레이트리밋(초과 429), body 길이 상한(class-validator MaxLength).
- ConversationModule을 AppModule에 연결. (가드 불필요 — 공개 엔드포인트)

## 변경 파일

- `packages/api/src/conversation/comment.service.ts` (listByPost)
- `packages/api/src/conversation/comment.service.spec.ts` (+2 케이스)
- `docs/tasks/blog-mvp.md` (status done)
