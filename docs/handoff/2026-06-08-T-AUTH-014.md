# Handoff — T-AUTH-014 (PATCH /api/auth/me bio 편집)

## 한 일
- 프로필 수정 엔드포인트가 소개(bio)를 수용하도록 확장 (author-profile, ADR-0028 = ADR-0025 amend). 신규 엔드포인트 없음 — 기존 `PATCH /api/auth/me` 동작 확장.
- 변경 파일:
  - `packages/api/src/auth/dto/update-profile.dto.ts` — `bio?: string | null` 추가. `@IsOptional` + `@ValidateIf(o => o.bio !== null)` + `@IsString` + `@MaxLength(200)` (avatarUrl 의 null 우회 패턴과 동형). null/빈 문자열 허용, 201자 거부.
  - `packages/api/src/auth/auth.service.ts` — `updateProfile` input 에 `bio?` 추가, `data` 에 `...(input.bio !== undefined && { bio: input.bio })`. select/return 의 bio 는 T-AUTH-012 에서 이미 포함.
  - `packages/api/src/auth/auth.controller.ts` — `updateMe` 가 `bio: dto.bio` 전달, 주석 갱신.
  - `packages/api/test/profile.e2e-spec.ts` — bio e2e 4건(편집 200+GET /me 왕복 / 201자 400 / null 200 / 빈문자열 200).
- shared `UpdateProfileDto.bio` · `AuthUserDto.bio` 는 T-AUTH-012 에서 이미 정의(이번 변경 없음, 규칙 #5 단일 정의 유지).
- `CLAUDE.md` AUTO-MANAGED 표의 `PATCH /api/auth/me` 설명을 "이름·아바타·소개"로 갱신.

## TDD 사이클 확인
- **RED**: profile.e2e-spec 에 bio 4건 추가 → 3건 실패(전역 ValidationPipe `forbidNonWhitelisted` 가 미화이트리스트 bio 를 400 거부 → 편집·null·빈문자열 테스트가 기대 200 대신 400). "201자→400"은 우연히 통과(같은 400).
- **GREEN**: DTO 에 bio 화이트리스트 + service data 반영 + controller 전달 → 17/17 통과.
- **REFACTOR**: controller 주석을 "이름·아바타·소개"로 정합.

## 결정한 것
- bio 검증은 api class-validator(@MaxLength 200), 폼 zod 는 web 인라인(ADR-0004, 함정 #9 — shared 순수성 유지). 새 ADR 불필요(ADR-0028 범위 내).
- null 우회는 `@ValidateIf(o => o.bio !== null)`(avatarUrl 동형). 빈 문자열 `''` 은 @IsString+@MaxLength 통과로 "소개 제거" 허용.
- 본인만 수정: `actor.id`(JWT 주체)로만 update, 클라이언트 id 미수용 → IDOR/mass-assignment 없음(전역 whitelist).

## 알려진 이슈
- 없음. 전체 회귀: api 단위 185/185, e2e 108/108 GREEN.
- code-reviewer Suggestion(선택, 미반영): bio trim 정규화(공백만 입력 시 비어보이지 않는 bio), 200자 경계값(딱 200→200 OK) 테스트. 기존 name/avatar 도 trim 안 하므로 일관성상 보류.

## 다음 세션이 알아야 할 것
- author-profile 남은 태스크(web): **T-WEB-402**(`/users/:id` AuthorProfile 페이지 + 작성자 링크, `useAuthorProfile(id)` + `usePosts({author})` 훅 — 이번 `GET /api/users/:id` + T-PUB-107 `?author` 소비), **T-WEB-403**(`/profile` bio textarea, useUpdateProfile 확장 — 이번 PATCH bio 소비). 둘 다 web 패키지.
- web 폼 zod 는 인라인/로컬(shared 금지). API 는 `src/api` 훅 경유, 직접 fetch 금지(web 규칙).

## 명령 출력 증거
- lint: 통과(무경고). typecheck: `tsc --noEmit -p tsconfig.json` → 에러 0(`TC_OK`).
- e2e: `pnpm --filter api test:e2e -- profile.e2e-spec` → **17 passed**. 전체 e2e → **108 passed / 15 suites**.
- 단위 전체: `pnpm --filter api test` → **185 passed / 26 suites**.
- verifier: acceptance 4/4 **PASS**(왕복·201→400·null·빈문자열, 무회귀). code-reviewer: Critical 0 / Warning 0 / Suggestion 2~3(전부 선택).

평가: Accept (12/12)
