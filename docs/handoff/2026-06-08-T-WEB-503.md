# Handoff — T-WEB-503 (시리즈 목록 /series)

## 한 일
- 공개 시리즈 목록 페이지 추가 (series, ADR-0029). `GET /api/series`(T-SER-004) 소비 + NavBar 진입점.
- 신규/변경 파일:
  - `packages/web/src/pages/SeriesList.tsx`(신규) — `useSeriesList({page,pageSize})` → 시리즈 카드 그리드(제목 `Link(/series/:slug)`·설명·작성자 `Link(/users/:authorId)`·`{postCount}편`) + `Pagination` + 로딩/에러/빈("아직 시리즈가 없습니다.") 상태.
  - `packages/web/src/series/useSeriesList.ts`(신규) — `GET /api/series` useQuery(page/pageSize params).
  - `packages/web/src/routes.tsx` — `/series` 등록(`/series/:slug` 앞).
  - `packages/web/src/components/NavBar.tsx` — 공개 네비에 "시리즈"(`/series`) NavLink 추가(발견성).
  - `packages/web/src/pages/SeriesList.test.tsx`(신규, 3).

## TDD 사이클 확인
- **RED**: SeriesList.test 3건 → SeriesList import 실패.
- **GREEN**: useSeriesList → SeriesList → 라우트 → 3건 통과.
- **REFACTOR**: NavBar 링크 추가(발견성). 전체 web 163 GREEN(NavBar 회귀 0).

## 결정한 것
- 시리즈 카드는 PostListView(글 카드)와 형태가 달라(시리즈 메타) SeriesList 자체 카드 그리드 렌더. 페이지네이션·빈 상태는 기존 패턴(TagPosts/PostList) 동형. 새 ADR 불필요.
- `/series` 라우트를 `/series/:slug` 앞에 배치(가독성, React Router v6는 정적 우선이라 순서 무관).
- 데이터 훅 경유(useSeriesList), 직접 fetch 0(web 규칙).

## 알려진 이슈
- 없음. web 163/163 GREEN, typecheck·lint 클린.
- code-reviewer Suggestion(선택): useSeriesList queryKey 기본값 정규화, setSearchParams 함수형 업데이트(향후 필터 추가 대비). 현재 무해.

## 다음 세션이 알아야 할 것
- series 마지막 태스크: **T-WEB-504**(작성자 시리즈 관리 UI — `/admin/series` 목록+생성, `/admin/series/:id/edit` 수정+글 선택·순서 지정 PUT, 삭제. POST/PATCH/DELETE/PUT posts 소비, deps T-SER-002·003 done). 끝나면 **series(E20) 기능 완결**.
- web 폼은 인라인 zod(shared 금지), 직접 fetch 금지(훅). 관리 화면은 AdminLayout 하위(AUTHOR/ADMIN). useSeriesAdmin 훅(create/update/delete/setPosts) 신규 필요.

## 명령 출력 증거
- 단위: SeriesList 3건 GREEN. 전체 `pnpm --filter web test` → **35 files / 163 tests passed**.
- typecheck: `tsc -b --noEmit` 통과. lint: `eslint .` 통과.
- verifier: acceptance 7단언 + typecheck/lint **PASS**. code-reviewer: Critical 0 / Warning 0 / Suggestion(선택).

평가: Accept (12/12)
