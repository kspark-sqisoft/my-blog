# Handoff — T-PUB-001 (PostService 생성/수정/삭제)

- 날짜: 2026-06-02
- 태스크: T-PUB-001
- Context: PUBLISHING
- status: done
- 의존: T-INFRA-002, T-INFRA-003 (done)

## 한 일

- `src/publishing/post.service.ts` — `PostService`:
  - `create({ title, contentMarkdown, authorId, tags? })` → status=DRAFT(기본), Tag connectOrCreate, PostDetailDto 반환.
  - `update(id, { title?, contentMarkdown?, tags? })` → 부분 수정, tags 주어지면 집합 교체(deleteMany+create).
  - `remove(id)` → 삭제. update/remove는 `ensureExists`로 없으면 NotFoundException.
  - `toDetail()`이 관계(postTags.tag.name)를 `tags: string[]`로, Date를 ISO 문자열로 매핑.
- 통합 테스트 `post.service.spec.ts` 5케이스.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | create → status=DRAFT, authorId 설정 | ✅ publishedAt=null 포함 |
| 2 | update title/contentMarkdown/tags 반영 | ✅ tags 집합 교체 |
| 3 | 없는 id update/remove → NotFound | ✅ (+remove 동작 검증) |

게이트: lint 0, tsc 0, unit 15/15, e2e 14/14.

## 경계 메모 (중요)

- **Tag 0~5 제한 검증과 `GET /api/tags`는 T-PUB-003 몫**. T-PUB-001은 tag를 connectOrCreate로 "연결"만 한다(검증 없음).
  - T-PUB-003 착수 시: 0~5 초과 BadRequest 검증을 PostService.create/update 경로(또는 TagService)에서 강제하고, Tag 목록 엔드포인트 추가. tagCreate 헬퍼를 TagService로 추출 검토.
- create는 `status` 인자를 받지 않음(항상 DRAFT). 발행 전이는 T-PUB-002(publish/unpublish).

## 다음 태스크가 알아야 할 것

- 반환 타입은 `@blog/shared`의 `PostDetailDto`.
- 발행 상태 전이: T-PUB-002 (publish→PUBLISHED+publishedAt, unpublish→DRAFT).
- 목록/상세 조회: T-PUB-004/005.
- 컨트롤러(T-PUB-006)에서 authorId = req.user.id (인증된 운영자), JwtAuthGuard 보호.

## 변경 파일

- `packages/api/src/publishing/post.service.ts` (신규)
- `packages/api/src/publishing/post.service.spec.ts` (신규)
- `docs/tasks/blog-mvp.md` (status done)
