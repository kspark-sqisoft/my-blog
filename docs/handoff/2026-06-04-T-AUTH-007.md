# Handoff — T-AUTH-007 (JwtStrategy DB 재조회 + AuthUserDto name·role)

날짜: 2026-06-04 / 기능: multiuser-rbac (Phase 1)

## 한 일
- `shared/dto/auth.ts`: `AuthUserDto`에 `name`·`role` 추가(가산 보류분 확정).
- `jwt.strategy.ts`: `PrismaService` 주입(@Global), `validate` async DB 재조회 → `{id,email,name,role}`. 삭제 계정(sub 없음) → 401.
- `auth.service.ts`: login 응답 user에 name·role 포함.
- 테스트: auth.service.spec(login name/role), auth.e2e(/me role/name) RED→GREEN. `auth-guard.e2e-spec.ts` 갱신(ProbeAuthModule에 PrismaModule import, 실 User 시드, "삭제 계정 → 401" 케이스 추가).

## 검증
- api 유닛 60 / e2e 36(+1) / typecheck / lint, 웹 34 / typecheck 통과.

## 설계 메모
- 역할은 토큰이 아닌 **DB 재조회**에서 옴 → 승격/강등 즉시 반영, 토큰 staleness 없음(ADR-0018). 비용은 요청당 PK 조회 1회.

## 다음
- T-AUTH-008: `@Roles`/`RolesGuard`(Reflector) + `OptionalJwtAuthGuard`, AuthModule providers+exports.
