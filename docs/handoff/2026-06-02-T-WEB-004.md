# Handoff — T-WEB-004 (Post 상세 + 마크다운 렌더 sanitize)

- 날짜: 2026-06-02
- 태스크: T-WEB-004
- Context: WEB
- status: done
- 의존: T-WEB-001, T-PUB-006 (done)

## 한 일

- `src/components/Markdown.tsx` — `react-markdown` + `rehype-sanitize`. 안전한 img만, 원시 HTML 미파싱(<script> 텍스트화) — ADR-0003/NF1.
- `src/posts/usePost.ts` — `GET /api/posts/:id`(초안/없음 404 → 에러 상태).
- `src/pages/PostDetail.tsx` — 제목·발행일(YYYY-MM-DD)·Tag 링크·마크다운 본문, 로딩/에러 처리.
- `routes.tsx`: `/posts/:id`→PostDetail (placeholder 교체).

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | react-markdown+rehype-sanitize, 안전한 img | ✅ |
| 2 | XSS 무력화 | ✅ script 요소 미생성, onerror 제거 |
| 3 | 발행일·Tag 표시 | ✅ |

게이트: lint 0, typecheck 0, test 19/19, build green. Docker 상세 API 확인.

## 다음 태스크가 알아야 할 것

- T-WEB-005(댓글): `GET/POST /api/posts/:postId/comments`. depth 0→1→2 중첩 렌더, 깊이 2 답글은 "답글" 비활성. 작성 폼(body 필수). 상세 페이지 하단에 통합.
- 백엔드 응답 CommentDto: `{ id, postId, parentId, depth, displayName, body, createdAt, replies[] }`.

## 변경 파일

- `packages/web/src/components/Markdown.tsx` (+ test, 신규)
- `packages/web/src/posts/usePost.ts` (신규)
- `packages/web/src/pages/PostDetail.tsx` (+ test, 신규)
- `packages/web/src/routes.tsx`
- `packages/web/package.json` (react-markdown, rehype-sanitize)
- `feature_list.json`, `docs/tasks/blog-mvp.md` (status done)
