# Handoff — T-SEO-005 (nginx 봇 UA 분기 + 산출물 프록시 + dev vite proxy)

날짜: 2026-06-08 / 기능: seo-feed (E17) / 컨텍스트: WEB(서빙 정합)

## 한 일
- prod nginx 와 dev vite 의 서빙 정합을 맞췄다(ADR-0026).
  - `packages/web/nginx.conf`:
    - `map $http_user_agent $is_crawler` — 대표 봇(facebookexternalhit·Twitterbot·kakaotalk·Slackbot·Discordbot·Googlebot·bingbot·LinkedInBot·WhatsApp·TelegramBot·Applebot·redditbot) 판별.
    - `location ~ ^/posts/(.*)$` — `if ($is_crawler) { proxy_pass http://api:3000/og/posts/$1; }` + `try_files $uri $uri/ /index.html`. **봇만 OG HTML, 사람은 SPA(영향 0)**.
    - `= /feed.xml`·`= /sitemap.xml`·`= /robots.txt`·`/og/` → api:3000 프록시.
    - proxy 공통 헤더(Host·X-Real-IP·X-Forwarded-For·X-Forwarded-Proto·http/1.1)를 **server 레벨로 이동**해 모든 location 상속(기존 /api·/uploads 동작 유지).
  - `packages/web/vite.config.ts`: proxy 에 `/feed.xml`·`/sitemap.xml`·`/robots.txt`·`/og` 추가(dev 도 api 프록시).

## TDD/검증 사이클 확인
- 인프라 설정이라 단위 TDD 대신 **nginx -t + 정합 + 회귀**로 검증.
- **nginx -t**: `docker run --rm --add-host api:127.0.0.1 -v nginx.conf:/etc/nginx/conf.d/default.conf:ro nginx:alpine nginx -t` → `syntax is ok / test is successful`.
- web lint·typecheck·test(131) 통과(회귀 0).
- 경로 정합: nginx/vite ↔ api `app-setup` exclude(feed/sitemap/robots/og) ↔ `og.controller`(/og/posts/:slug) 네 곳 일치(code-reviewer·verifier 교차 확인).

## 결정한 것
- 새 ADR 불필요(ADR-0026). 봇 분기는 `if ($is_crawler) proxy_pass + try_files` 공존 패턴(nginx 안전 사용역).
- **사용자 합의**: 실제 봇/사람 컨테이너 스모크(prod web 이미지+api+db 통합)는 무거운 빌드라 이번 범위 외 — **prod 배포·격리 e2e 후속 항목**. acceptance #4 를 "nginx -t + 설정 리뷰"로 조정(feature_list·tasks 동기).

## 알려진 이슈 / 후속
- **봇/사람 컨테이너 스모크(후속 필수)**: prod 스택에서 `curl -A "Googlebot" /posts/:slug` → OG HTML, 일반 UA → index.html(`<div id="root">`) 각 1회 확인. "if is evil" 패턴이라 실트래픽 동작은 배포 검증에서 최종 확인 권장.
- map UA 목록은 운영 중 누락 봇 발견 시 보강(ADR-0026 Consequences).

## 다음 세션이 알아야 할 것
- 다음 태스크: **T-SEO-006** (web `index.html` 기본 OG 메타 + 피드 자동발견 link + `public/og-default.png` 사이트명 PNG 생성, deps 없음). seo-feed 의 **마지막 태스크** — 완료 시 E17 전체 done.
- 기본 OG 이미지(`/og-default.png`)는 T-SEO-004 OgMetaService 폴백이 참조 중 — T-SEO-006 이 실제 자산을 배치해야 200 서빙.

## 명령 출력 증거
- nginx -t: `configuration file test is successful`
- web: lint 무오류 / typecheck(tsc -b --noEmit) 무오류 / test 131 passed
- verifier: 1·2·3 PASS, 4 조정범위 PASS(스모크 후속) / code-reviewer: Critical·Warning 0
