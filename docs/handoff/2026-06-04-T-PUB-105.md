# Handoff — T-PUB-105 (Post 소유권 + 컨트롤러 @Roles) · Phase 1 완료

날짜: 2026-06-04 / 기능: multiuser-rbac (Phase 1)

## 한 일
- `post.service.ts`: `Actor{id,role}` 도입. `update/remove/publish/unpublish`가 actor 수용, `assertCanMutate`(ADMIN 전체 / AUTHOR 본인 / 그 외 403). `requirePost`로 먼저 404, 그다음 403. `ensureExists` 제거.
- `post.controller.ts`: 쓰기 핸들러 `@UseGuards(JwtAuthGuard, RolesGuard) @Roles('AUTHOR','ADMIN')` + `actorOf(req)` 전달.
- `admin-post.controller.ts`: `@Roles('ADMIN')`(다른 작성자 초안 노출 → ADMIN 한정).
- `upload.controller.ts`: `@Roles('AUTHOR','ADMIN')`.
- 테스트: post.service.spec 소유권 4케이스(본인/타인403/ADMIN전체/404우선) + 기존 호출 actor 전달. post.e2e 역할 4케이스(MEMBER 403, AUTHOR 본인 201/200, AUTHOR 타인 403, ADMIN 개입 200). 시그니처 변경에 따라 tag.service.spec·tag.e2e·admin-posts.e2e 호출부 actor 보정.

## 검증
- api 유닛 69 / e2e 40 / typecheck / lint 통과.

## Phase 1 완료
- T-AUTH-005·006·007·008·T-PUB-105 모두 done. 백엔드 RBAC 기반(스키마·역할·가드·소유권) 완성. 기존 운영자=ADMIN으로 동작 동일.

## 다음 (Phase 2)
- T-AUTH-009(회원가입 register), T-AUTH-010(사용자 관리 API).
