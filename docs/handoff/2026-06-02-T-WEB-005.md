# Handoff — T-WEB-005 (댓글 UI: 깊이 2 중첩 + 작성 폼)

- 날짜: 2026-06-02
- 태스크: T-WEB-005
- Context: WEB
- status: done
- 의존: T-WEB-004, T-CONV-003 (done)

## 한 일

- `src/comments/useComments.ts` — `GET /api/posts/:postId/comments`(중첩).
- `src/comments/useCreateComment.ts` — `POST .../comments` mutation, 성공 시 `['comments', postId]` 무효화.
- `src/components/CommentForm.tsx` — body(필수)+이름(선택), parentId 있으면 답글. body 비면 등록 비활성.
- `src/components/CommentTree.tsx` — 재귀 렌더, depth<2 에만 "답글" 토글(폼). 깊이 2엔 답글 없음(ADR-0013).
- `src/components/CommentSection.tsx` — 폼 + 로딩/에러/목록. PostDetail 하단에 통합.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | depth 0→1→2 중첩, 깊이 2 답글 버튼 없음 | ✅ (버튼 수 = depth<2 노드 수) |
| 2 | 작성 → POST, 목록 갱신 | ✅ mutation invalidate |
| 3 | body 빈 경우 제출 차단 | ✅ 등록 비활성 |

게이트: lint 0, typecheck 0, test 24/24, build green.
Docker 실환경: depth0→1→2 댓글 작성·조회 정상(한글 정상).

## 메모

- PostDetail.test 의 api.get 모킹을 URL 기반(`mockImplementation`)으로 변경 — 본문 + /comments 둘 다 처리.
- 댓글은 익명(공개) — 가드 없음. 백엔드 throttler가 스팸 억제(429).

## 다음 태스크가 알아야 할 것

- T-WEB-006(운영자 대시보드): 초안 포함 목록이 필요. 현재 공개 `GET /api/posts`는 발행글만 → 운영자용 초안 목록 엔드포인트가 없음.
  → **백엔드에 운영자 전용 목록(초안 포함)이 필요할 수 있음**(현 범위 밖). 우회: 작성/발행/삭제 액션 중심으로 구성하거나, 별도 태스크로 운영자 목록 API 추가 검토.

## 변경 파일

- `packages/web/src/comments/{useComments,useCreateComment}.ts` (신규)
- `packages/web/src/components/{CommentForm,CommentTree,CommentSection}.tsx` (+ Form/Tree test, 신규)
- `packages/web/src/pages/PostDetail.tsx`, `PostDetail.test.tsx`
- `feature_list.json`, `docs/tasks/blog-mvp.md` (status done)
