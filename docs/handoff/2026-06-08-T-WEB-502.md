# Handoff — T-WEB-502 (글 상세 시리즈 네비 UI)

## 한 일
- 글 상세에 시리즈 네비게이션 UI 추가 (series, ADR-0029). `PostDetailDto.series`(T-SER-005)를 소비.
- 신규/변경 파일:
  - `packages/web/src/components/SeriesNav.tsx`(신규) — props `series: SeriesNavDto`. `<nav aria-label="시리즈 네비게이션">` 안에 시리즈 제목 `Link(/series/:slug)` + `{position}/{total}편` + 이전/다음 글 `Link(/posts/:slug)`. prev/next null이면 해당 링크 미렌더.
  - `packages/web/src/pages/PostDetail.tsx` — header 뒤에 `{post.series && <SeriesNav series={post.series} />}`.
  - `packages/web/src/index.css` — `.ab-series-nav*` 스타일(인라인 style 아님, 기존 `ab-text-link`/`ab-dot` 재사용).
  - 테스트: `SeriesNav.test.tsx`(신규 4), `PostDetail.test.tsx`(시리즈 소속 노출·미소속 미표시 2 + detail() 헬퍼에 `series: null` 계약 정합).

## TDD 사이클 확인
- **RED**: SeriesNav.test 4건 → SeriesNav import 실패.
- **GREEN**: SeriesNav 컴포넌트 + PostDetail 연결 → SeriesNav 4 + PostDetail 통합 2 통과.
- **REFACTOR**: code-reviewer Warning 반영 — PostDetail.test `detail()` 헬퍼에 `series: null` 추가(PostDetailDto.series required 계약 정합). CSS 추가.

## 결정한 것
- 네비는 상세 헤더 뒤(본문 위) 배치 — "지금 N/M편 읽는 중" 동선. 새 ADR 불필요.
- prev/next는 null이면 숨김(첫 글 prev 없음·끝 글 next 없음). 접근가능 이름 = "이전 글: {title}"/"다음 글: {title}", 시리즈 제목 링크.
- `post.series && ...` 가드로 미소속 안전 미표시.

## 알려진 이슈
- 없음. web 160/160 GREEN, typecheck·lint 클린.
- code-reviewer Suggestion(선택): 첫/끝 글 검증을 텍스트 대신 `queryByRole('link')`로(접근성 계약 직결), N/M편 단언 견고화. 현재도 acceptance 충족.

## 다음 세션이 알아야 할 것
- 남은 series 태스크: **T-WEB-503**(시리즈 목록 `/series` — `GET /api/series` 소비, useSeriesList 훅, deps T-SER-004 done), **T-WEB-504**(작성자 시리즈 관리 UI — POST/PATCH/DELETE/PUT posts 소비, deps T-SER-002·003 done). 둘 끝나면 series(E20) 기능 완결.
- `useSeries` 훅 패턴(src/series/)·`PostListView` items 모드가 이미 있어 T-WEB-503에서 재사용 가능.

## 명령 출력 증거
- 단위: SeriesNav 4 + PostDetail 시리즈 2 GREEN. 전체 `pnpm --filter web test` → **34 files / 160 tests passed**.
- typecheck: `tsc -b --noEmit` 통과. lint: `eslint .` 통과.
- verifier: acceptance 전 단언 **PASS**(nav aria-label·링크 href·prev/next 숨김·미소속 미표시·회귀 0). code-reviewer: Critical 0 / Warning 2(detail() series:null 반영, N/M편 단언 견고화 선택) / Suggestion(접근성 강화).

평가: Accept (12/12)
