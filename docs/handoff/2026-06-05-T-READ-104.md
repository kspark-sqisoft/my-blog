# Handoff — T-READ-104 (관련 글 API + shared 타입 + web 섹션)

읽기 경험(B) 마지막 태스크. 글 하단에 태그 겹침 기반 관련 글 표시.

## 한 일
- shared `RelatedPostDto`(id·slug·title·tags·publishedAt·coverImageUrl) 정의 + index export.
- api `PostService.getRelated(idOrSlug, limit=4)`:
  - 소스 발행글 조회(없으면 404). 공유 태그 수 desc → publishedAt desc 정렬, 자기 제외.
  - 부족분은 최신 발행글로 보완. 발행글만. `toRelated` 매퍼(대표 이미지 추출 재사용).
- api `GET /api/posts/:id/related?limit=` 컨트롤러 추가(`:id` 상세 라우트보다 먼저 선언해 충돌 방지).
- web `useRelatedPosts(idOrSlug, limit=4)` 훅 + `RelatedPosts` 섹션(비거나 로딩/에러면 `null` → 숨김).
- web `PostCardCover` 컴포넌트 추출(목록·관련글 카드 커버 공용, 중복 제거) → `PostListView` 도 사용.
- `PostDetail` 에 관련 글 섹션 연결(본문 아래, 댓글 위). `index.css` 관련 섹션 스타일.
- CLAUDE.md AUTO-MANAGED 엔드포인트 표에 `/api/posts/:id/related` 추가.

## TDD 사이클 확인
- RED: `post.service.spec`(getRelated 6) + `post.e2e-spec`(related 2) → `service.getRelated is not a function`.
       web `RelatedPosts.test`(3) → 모듈 부재 import 실패.
- GREEN: shared 타입 + 서비스 + 컨트롤러 + 훅/컴포넌트 구현 →
         api 단위 38(post.service) / web 단위 118 통과.
- REFACTOR: PostCardCover 추출(목록 카드 중복 제거), expect.any → 구체 단정(lint no-unsafe-assignment 해소).

## 결정한 것
- 관련도는 공유 태그 수 우선, 부족분 최신 보완(ADR-0023 범위). 추천 고도화(인기/임베딩)는 후속.
- 관련 글은 캐시 없이 매 조회 계산(블로그 규모에서 충분, YAGNI).
- 카드 커버 로직은 PostCardCover 로 단일화.

## 알려진 이슈
- 첫 web 격리 e2e 시도가 **네트워크 오류**로 실패(docker 빌드 중 `prisma generate` 가 binaries.prisma.sh
  DNS 실패). 코드 무관 — 재시도로 해결. (api e2e 68 은 test DB 직접 실행으로 이미 통과.)

## 다음 세션이 알아야 할 것
- 읽기 경험(B) feature 완료(T-READ-101~104 done). 다음 로드맵: C(참여 — 좋아요/조회수) 또는 D(작성자 — 예약발행/자동저장).
- highlight.js(T-READ-102) 의존성 추가분 dev 컨테이너 재빌드 필요분이 남아 있으면 함께 처리.

## 명령 출력 증거
- api lint: 무오류. api 단위: `Tests 133 passed`(post.service 38 포함). api e2e: `Test Suites 10 / Tests 68 passed`.
- web lint/typecheck: 무오류. web 단위: `Test Files 27 / Tests 118 passed`.
- web e2e(격리, 재시도): `7 passed (20.6s)`, dev DB 무영향(Post:Comment 32:99 불변).
