# Handoff — T-AUTH-003 (JwtStrategy + JwtAuthGuard)

- 날짜: 2026-06-02
- 태스크: T-AUTH-003
- Context: AUTH
- status: done
- 의존: T-AUTH-002, T-INFRA-004 (done)
- 관련: ADR-0001

## 한 일

- `src/auth/jwt.strategy.ts` — passport-jwt 전략:
  - **쿠키 `access_token`** 에서 JWT 추출(cookieExtractor), `ignoreExpiration: false`.
  - `validate(payload)` → `req.user = { id: payload.sub, email: payload.email }`.
- `src/auth/jwt-auth.guard.ts` — `AuthGuard('jwt')` 확장.
- e2e `test/auth-guard.e2e-spec.ts` — 보호 프로브 라우트로 4케이스 검증.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | access_token 쿠키에서 JWT 추출·검증 | ✅ 유효 토큰 200 |
| 2 | 유효 토큰 → req.user{ id, email } | ✅ {id, email} 반환 |
| 3 | 없음/위조/만료 → 401 | ✅ 3케이스 401 |

게이트: lint 0, tsc 0, unit 10/10, e2e 9/9.

## 부수 수정 (테스트 인프라)

- **`--runInBand` 적용** (test/test:watch/test:cov/test:e2e):
  통합 테스트가 단일 Postgres를 공유하는데 여러 suite가 병렬로 `user.deleteMany()`+seed 하여
  경합(같은 `owner@example.com`)이 발생 → 직렬 실행으로 고정. 이후 모든 DB 통합 테스트의 전제.

## 다음 태스크가 알아야 할 것 (T-AUTH-004)

- AuthModule에서 `PassportModule`, `JwtModule.registerAsync`(JWT_SECRET 주입), `AuthService`, `JwtStrategy`, `AuthController` 와이어링.
- 컨트롤러 `POST /api/auth/login`: AuthService.login → `res.cookie('access_token', token, { httpOnly, sameSite:'lax', secure })` (ADR-0001).
- `GET /api/auth/me`: `@UseGuards(JwtAuthGuard)` → `req.user` 반환.
- `secretOrKey`는 현재 `process.env.JWT_SECRET ?? 'change-me'`. 운영에서 JWT_SECRET 필수 — AuthModule/config에서 보장 권장.

## 변경 파일

- `packages/api/src/auth/jwt.strategy.ts` (신규)
- `packages/api/src/auth/jwt-auth.guard.ts` (신규)
- `packages/api/test/auth-guard.e2e-spec.ts` (신규)
- `packages/api/package.json` (test 스크립트 --runInBand)
- `docs/tasks/blog-mvp.md` (status done)
