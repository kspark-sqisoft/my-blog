# Handoff — T-READ-101 (읽는 시간 유틸 + 헤더 표시)

읽기 경험(B단계) 첫 태스크. ADR-0023(렌더타임 클라이언트 변환) 기반.

## 한 일
- `packages/web/src/lib/reading-time.ts` — `estimateReadingTime(html)` 순수 함수 신설.
  - HTML 태그 제거 + 최소 엔티티 디코드(`&nbsp;`·`&amp;` 등) 후 공백 제외 글자 수 → 분(500자/분, 올림).
  - 빈 본문/태그만 → 0(호출부에서 숨김), 그 외 최소 1분.
- `packages/web/src/pages/PostDetail.tsx` — 헤더 meta 에 "N분 읽기" 표시.
  - `useMemo` 로 `contentHtml || contentMarkdown` 에서 1회 계산.
  - 준비돼 있던 미사용 CSS 클래스 `.ab-dot`(구분점)·`.ab-meta-read` 활용(저자 · 날짜 · N분 읽기).
  - 0분이면 분기 숨김(빈 글 보호).

## TDD 사이클 확인
- RED: `reading-time.test.ts` 작성 → 모듈 부재로 import 실패
  (`Failed to resolve import "./reading-time"`), 기존 98 통과 유지.
- GREEN: `reading-time.ts` 구현 → reading-time 5 케이스 포함 103 통과.
- REFACTOR: 엔티티 디코드 테이블 분리, 주석 정리. 추가 변경 없음(테스트 그대로 통과).

## 결정한 것
- 파생 표현(읽는 시간)은 DB 저장 없이 렌더타임 계산 — ADR-0023 에 기록(신규 ADR, 이 feature 공통).
- 한글 기준 500자/분 고정(영어 단어 가중치는 YAGNI 생략).
- 글자 수는 "공백 제외"로 계산(엔티티·다중 공백 정규화).

## 알려진 이슈
- 영어 위주 본문은 글자 기준이라 실제 읽기 속도와 다소 차이날 수 있음(트레이드오프, 추후 보완 여지).

## 다음 세션이 알아야 할 것
- 다음 태스크: **T-READ-102**(코드 하이라이트 자동감지 + 헤딩 id 부여, `enhanceArticleHtml`).
  - sanitize 로직을 `lib/sanitize-rich-html.ts` 로 추출해 `RichContent` 와 공유 예정.
  - highlight.js 동적 import 로 번들 분리.
- feature: reading-experience (E14). 의존: T-READ-103 → 102.

## 명령 출력 증거
- lint: `eslint .` 무출력(통과).
- typecheck: `tsc -b --noEmit` 무오류(통과).
- unit: `Test Files 23 passed (23)`, `Tests 103 passed (103)`.
- e2e: `pnpm test:e2e`(격리) — `7 passed (31.4s)`, dev DB 무영향(Post:Comment 32:99 불변).
