# Handoff — T-CONV-003 (CommentController + Throttler)

- 날짜: 2026-06-02
- 태스크: T-CONV-003
- Context: CONVERSATION
- status: done
- 의존: T-CONV-002 (done)
- 관련: ADR-0009, ADR-0013, NF1

## 한 일

- `CreateCommentDto`: body(@MinLength 1, @MaxLength 1000), displayName?(@MaxLength 50), parentId?.
- `CommentController`(`/api/posts/:postId/comments`, 공개):
  - `GET /` → listByPost(중첩).
  - `POST /`(익명) → create. `@UseGuards(ThrottlerGuard)`로 레이트리밋(POST만).
- `ConversationModule`: `ThrottlerModule.forRoot([{ ttl: 60000, limit: 10 }])` + CommentController/Service.
- AppModule에 ConversationModule 연결.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | GET/POST 공개, 200/201 | ✅ 중첩 GET, 작성 201 |
| 2 | Throttler 초과 429 | ✅ (POST만, GET 미적용) |
| 3 | body 누락/길이(1000) 초과 400 | ✅ |

게이트: lint 0, tsc 0, unit 41/41, e2e 29/29.

## E4(Conversation) + 백엔드 API 완료

전체 도메인 API 완비:
- Auth: login/logout/me
- Publishing: posts CRUD·발행·목록·상세, tags, uploads
- Conversation: comments(깊이 2) 목록·작성(레이트리밋)

## 다음 단계 (E5 프론트엔드)

- T-WEB-001 API클라(axios withCredentials)+라우터+Query/Tailwind → 002 로그인/보호라우트 → 003 목록 → 004 상세+마크다운(sanitize) → 005 댓글(깊이2) → 006 운영자 대시보드 → 007 에디터+이미지 → 008 Playwright E2E.
- 프론트는 `@blog/shared` 타입 사용, web=zod 검증(ADR-0004).
- 주의: web 패키지엔 아직 테스트 인프라(vitest 설정) 확인 필요.

## 변경 파일

- `packages/api/src/conversation/dto/create-comment.dto.ts` (신규)
- `packages/api/src/conversation/comment.controller.ts` (신규)
- `packages/api/src/conversation/conversation.module.ts` (신규)
- `packages/api/src/app.module.ts` (ConversationModule import)
- `packages/api/package.json` (@nestjs/throttler)
- `packages/api/test/comment.e2e-spec.ts` (신규)
- `docs/tasks/blog-mvp.md` (status done)
