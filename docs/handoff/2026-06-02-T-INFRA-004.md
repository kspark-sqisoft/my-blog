# Handoff — T-INFRA-004 (전역 ValidationPipe + 예외 포맷 + cookie-parser)

- 날짜: 2026-06-02
- 태스크: T-INFRA-004
- Context: INFRA
- status: done
- 의존: T-INFRA-002 (done)

## 한 일

- `src/common/app-setup.ts` — `configureApp(app)`:
  - `setGlobalPrefix('api')` (TRD §3, 모든 라우트 /api 하위)
  - `cookieParser()` (httpOnly 쿠키 파싱 — ADR-0001 대비)
  - 전역 `ValidationPipe({ whitelist, forbidNonWhitelisted, transform })`
- `main.ts`가 `configureApp` 사용 (+ `void bootstrap()` 부유 프라미스 경고 해소).
- e2e 하니스 보정: `jest-e2e.json`에 `.js` moduleNameMapper + `setupFiles`(DATABASE_URL 기본값).
- 기존 `app.e2e-spec.ts`를 `/api` + `configureApp`으로 정합.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | ValidationPipe whitelist+forbidNonWhitelisted 전역 | ✅ 유효 통과 / 추가속성 400 |
| 2 | 검증 실패 400 `{statusCode,message,error}` | ✅ (Nest 기본 예외 응답이 해당 형태) |
| 3 | cookie-parser로 req.cookies | ✅ 쿠키 echo 테스트 |

게이트: lint 0 errors, `tsc --noEmit` 0, unit 4/4, e2e 5/5.

## 다음 태스크가 알아야 할 것

- **모든 컨트롤러 경로는 `/api` 프리픽스 하위**다. 컨트롤러에는 프리픽스 없는 경로만 선언(예: `@Controller('posts')` → 실제 `/api/posts`).
- 새 e2e 스펙은 `configureApp(app)`를 적용해 동일한 전역 설정으로 테스트할 것.
- e2e는 DB 필요(AppModule 부팅 시 Prisma): db 컨테이너 기동 + DATABASE_URL(기본 localhost:5433, setup에서 주입).
- 예외 응답 형태는 Nest 기본 필터가 `{statusCode,message,error}`를 제공 → 별도 커스텀 필터 불필요(필요 시 추후 추가).
- 검증 DTO는 class-validator 데코레이터 사용. `@blog/shared` 타입을 구현 기준으로 삼되 검증 규칙은 api에서 선언(ADR-0004).

## E0(기반) 완료

T-INFRA-001~004 모두 done → E1(인증) 착수 가능.

## 변경 파일

- `packages/api/src/common/app-setup.ts` (신규)
- `packages/api/src/main.ts`
- `packages/api/test/validation.e2e-spec.ts` (신규)
- `packages/api/test/jest-e2e.setup.ts` (신규)
- `packages/api/test/jest-e2e.json` (moduleNameMapper, setupFiles)
- `packages/api/test/app.e2e-spec.ts` (/api 정합)
- `packages/api/package.json` (cookie-parser, @types/cookie-parser)
- `pnpm-lock.yaml`
- `docs/tasks/blog-mvp.md` (status done)
