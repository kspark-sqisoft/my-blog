# Handoff — T-WEB-008 (Playwright E2E 핵심 플로우) · 🎉 MVP 30/30 완료

- 날짜: 2026-06-02
- 태스크: T-WEB-008 (마지막)
- Context: WEB
- status: done
- 의존: T-WEB-005, T-WEB-007 (done)

## 한 일

라이브 dev 스택(web:5173 / api:3001 / db:5433, Docker)에 대해 Playwright E2E를 추가.

- `packages/web/playwright.config.ts` — testDir `e2e`, baseURL `http://127.0.0.1:5173`,
  workers 1·직렬(공유 dev DB 상태 변경), reporter list, chromium 프로젝트.
- `e2e/helpers.ts` — `loginAsOperator`(UI 로그인), `OPERATOR`(시드 계정 owner@example.com),
  `PIXEL_PNG`(1x1 PNG 버퍼, 업로드용).
- `e2e/operator-flow.spec.ts` — 로그인→새 글 작성→이미지 업로드(본문에 `![]()` 삽입 검증)
  →저장(초안)→발행(상태 '발행됨')→공개 목록 노출.
- `e2e/reader-comment.spec.ts` — 목록→상세→최상위 댓글→그 댓글에 답글(깊이 2) 중첩 표시.
- `e2e/draft-hidden.spec.ts` — 초안 저장 후 공개 목록 미노출 + 초안 상세 직접 접근 시 에러(role=alert).

## 설정 변경

- `vite.config.ts` test.exclude 에 `**/e2e/**` 추가 → Vitest가 Playwright 스펙을 줍지 않음.
- `eslint.config.js` — `e2e/**`, `playwright.config.ts` 에 Node globals 오버라이드(Buffer/process).
- `package.json` 스크립트 `test:e2e: playwright test` 추가.
- `.gitignore` — `test-results/`, `playwright-report/` 제외.

## Acceptance 검증 (3/3 ✅) — 실제 브라우저 실행

```
✓ draft-hidden  초안 글은 공개 목록과 상세에서 숨겨진다 (3.3s)
✓ operator-flow 운영자가 글을 작성하고 이미지를 올린 뒤 발행한다 (1.4s)
✓ reader-comment 독자가 글을 열고 댓글과 답글(깊이 2)을 남긴다 (840ms)
3 passed (8.3s)
```

게이트: lint 0, typecheck 0, unit 32/32(e2e 제외), build green, **E2E 3/3 passed**.

## 실행 방법 / 주의

- 사전 조건: dev 스택 기동 필요 — `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d`.
- 실행: `pnpm --filter web test:e2e`. 브라우저 미설치 시 `pnpm --filter web exec playwright install chromium`.
- E2E는 **공유 dev DB(blog)** 상태를 변경한다(고유 제목/타임스탬프로 충돌 회피). 운영자 시드 계정 필요
  (`.env.example` 기본값 owner@example.com / change-me). jest 통합 테스트의 blog_test 격리와는 별개.

## 상태

- **MVP 전체 30/30 done.** `feature_list.json` 기준 남은 todo 없음.
- 후속 후보(범위 외): S3 스토리지 어댑터 전환(ADR-0012), 마크다운 이미지 sanitize 허용 태그 점검,
  E2E의 blog_test 격리 스택 분리, CI 파이프라인(GitHub Actions)에서 lint/test/e2e 자동화.
