# Handoff — T-PUB-103 (목록 요약 평문화 + 대표 이미지 파생)

- 날짜: 2026-06-04
- 태스크: T-PUB-103
- Context: PUB (Publishing)
- status: done
- 의존: T-PUB-009 (목록 API, done)
- 기능: apple-redesign

## 한 일

목록 응답(`GET /api/posts`)이 각 글의 **대표 이미지**와 **평문 요약**을 노출하도록 Publishing 읽기 모델을 확장.

- `packages/shared/src/dto/post.ts` — `PostSummaryDto.coverImageUrl: string | null` 추가.
- `packages/api/src/publishing/cover-image.ts` — `extractFirstImageUrl()`: 본문 마크다운 `![](url)`/원시 `<img src>` 중 먼저 나오는 이미지 URL 추출(없으면 null).
- `packages/api/src/publishing/markdown-summary.ts` — `toSummaryText()`: 코드펜스/이미지/링크/헤딩/인용/리스트/인라인코드/강조 제거 후 공백 정리·길이 제한(200).
- `post.service.ts` `toSummary()` — 위 두 헬퍼로 `summary`(평문) + `coverImageUrl`(파생) 생성. 목록 쿼리는 이미 본문 포함이라 **추가 DB 조회 없음**.

## TDD 사이클 확인

- **RED**: `cover-image.spec.ts` 작성 → 모듈 부재 import 실패.
- **GREEN**: `cover-image.ts` 구현 → 6 passed.
- **RED→GREEN**: `markdown-summary.spec.ts`(4) + `markdown-summary.ts` 구현 → 합산 10 passed.
- **REFACTOR**: `toSummary()` 의 인라인 요약 로직을 `toSummaryText()` 호출로 정리.

## 결정한 것

- **ADR-0015 (Proposed)** — 대표 이미지·요약을 저장 컬럼 없이 **읽기 시 본문에서 파생**(마이그레이션 없음, 본문이 단일 원천). 사용자 승인 대기.
- 대표 이미지는 "본문 첫 이미지"로 고정(운영자 수동 지정 없음 — ADR-0015 트레이드오프, PRD 오픈 이슈).

## 알려진 이슈

- 통합 스펙(`post.service.spec.ts`에 추가한 coverImageUrl 검증)과 api e2e는 테스트 DB(blog_test) 스택이 필요 — 이 세션에선 순수 단위(10)만 로컬 실행. CI/`pnpm --filter api test`에서 확인 필요.
- 매 목록 조회 시 본문 정규식 파싱(저볼륨이라 무시 가능).

## 다음 세션이 알아야 할 것

- API를 Docker(`my-blog-api-1`)로 띄운 경우 소스 변경이 핫리로드에 안 잡힐 때가 있음 → `docker restart my-blog-api-1` 로 재컴파일.
- 프론트 렌더는 T-WEB-010 참고.

## 명령 출력 증거

```
api unit: Test Suites 2 passed, Tests 10 passed (cover-image 6 + markdown-summary 4)
api typecheck: tsc --noEmit -p tsconfig.json → 0
live(재기동 후): GET /api/posts → items[].coverImageUrl="/uploads/9ef1...jpg", summary 평문 확인
```

commit: `a5a236d feat: 글 목록 카드에 대표 이미지(본문 첫 이미지) 표시`, `08995d0 fix(api): 목록 요약을 평문으로 정리`
