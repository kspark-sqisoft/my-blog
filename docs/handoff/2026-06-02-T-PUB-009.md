# Handoff — T-PUB-009 (운영자 Post 단건 조회 GET /api/admin/posts/:id)

- 날짜: 2026-06-02
- 태스크: T-PUB-009 (T-WEB-007 편집 로드 선행)
- Context: PUBLISHING
- status: done
- 의존: T-PUB-001, T-AUTH-003 (done)

## 한 일

- `PostService.getForAdmin(id)` → `requirePost`(관계 포함 조회+NotFound) 재사용해 초안 포함 PostDetailDto 반환.
- `AdminPostController` `GET /api/admin/posts/:id` (JwtAuthGuard) 추가.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | JwtAuthGuard 미인증 401 | ✅ |
| 2 | 초안도 contentMarkdown+status | ✅ PostDetailDto |
| 3 | 없는 id 404 | ✅ |

게이트: lint 0, tsc 0, unit 45/45, e2e 34/34.

## 다음 태스크가 알아야 할 것 (T-WEB-007)

- 편집 로드: `GET /api/admin/posts/:id`(쿠키 인증) → 제목/본문/Tag 초기값.
- 생성 `POST /api/posts`, 수정 `PATCH /api/posts/:id`.
- 이미지 업로드 `POST /api/uploads`(multipart) → URL 을 본문 마크다운에 삽입.
- Tag 6개 이상 클라 검증(서버 400과 일치).

## 변경 파일

- `packages/api/src/publishing/{post.service.ts,admin-post.controller.ts}`
- `packages/api/src/publishing/post.service.spec.ts`, `test/admin-posts.e2e-spec.ts`
- `feature_list.json`, `docs/tasks/blog-mvp.md`
