# Handoff — T-PUB-002 (발행/발행취소 상태 전이)

- 날짜: 2026-06-02
- 태스크: T-PUB-002
- Context: PUBLISHING
- status: done
- 의존: T-PUB-001 (done)
- 관련: ADR-0005

## 한 일

- `PostService.publish(id)` / `unpublish(id)` 추가:
  - publish: status=PUBLISHED + `publishedAt=new Date()`. **멱등** — 이미 발행이면 publishedAt 유지.
  - unpublish: status=DRAFT + `publishedAt=null`. **멱등** — 이미 초안이면 no-op.
  - `requirePost(id)`(관계 포함 조회 + 존재 보장) 헬퍼 추가.
- 스펙 5케이스 추가(총 10).

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | publish → PUBLISHED + publishedAt | ✅ |
| 2 | unpublish → DRAFT + publishedAt 정리 | ✅ null |
| 3 | 무의미한 전이 안전 처리 | ✅ 재발행 멱등(publishedAt 유지), 초안 unpublish no-op, 없는 id NotFound |

게이트: lint 0, tsc 0, unit 20/20, e2e 14/14.

## 다음 태스크가 알아야 할 것

- 목록(T-PUB-004)은 `status=PUBLISHED`만 `publishedAt` 최신순. 상세(T-PUB-005)는 초안 비노출.
- 컨트롤러(T-PUB-006): `POST /api/posts/:id/publish`, `/unpublish` → publish/unpublish 호출, JwtAuthGuard 보호. 이벤트 PostPublished/Unpublished는 MVP에선 로깅/생략 가능(도메인 이벤트 미구현 상태).

## 변경 파일

- `packages/api/src/publishing/post.service.ts` (publish/unpublish/requirePost)
- `packages/api/src/publishing/post.service.spec.ts` (+5 케이스)
- `docs/tasks/blog-mvp.md` (status done)
