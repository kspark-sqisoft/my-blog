# Handoff — T-AUTH-009 (회원가입 register)

날짜: 2026-06-04 / 기능: multiuser-rbac (Phase 2)

## 한 일
- `dto/register.dto.ts`: RegisterDto(email, password 8~72, name 1~50). role 필드 없음 → whitelist가 role 주입 거부.
- `auth.service.ts`: `register()` — 중복 email `ConflictException(409)`, bcrypt(SALT_ROUNDS=10), MEMBER 생성, 즉시 토큰. login·register 공통 `issue()` 헬퍼로 토큰+user 구성.
- `auth.controller.ts`: `POST /api/auth/register`(201) + 공통 `setAuthCookie`. login도 동일 헬퍼 사용.
- `CLAUDE.md` 엔드포인트 표에 register 추가.
- auth.e2e: 성공(201/MEMBER/쿠키)·중복 409·짧은 비번 400·role 주입 400 RED→GREEN.

## 검증
- api 유닛 69 / e2e 44(+4) / typecheck / lint 통과.

## 다음
- T-AUTH-010: 사용자 관리 API(GET /api/admin/users, PATCH /:id/role, 마지막 ADMIN 보호).
