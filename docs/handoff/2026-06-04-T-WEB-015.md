# Handoff — T-WEB-015 (댓글 폼 실명 자동 + CommentTree authorName)

날짜: 2026-06-04 / feature: multiuser-rbac / epic: E9 (S9.4)

## 한 일
- `CommentForm`: 로그인 회원이면 이름(선택) 입력을 숨기고 계정 이름(`user.name`)을 표시,
  제출 시 `displayName` 을 보내지 않음(서버가 계정 이름으로 기록 — ADR-0018). 비로그인은 기존대로 익명 이름 입력.
- `CommentTree`: 표시 이름 우선순위를 `authorName ?? displayName ?? '익명'` 으로 변경(로그인=실명 우선).
- CSS: 로그인 표시용 `.ab-cform-asname` 추가(`index.css`).
- e2e: `member-journey.spec.ts` 신규 — 가입→실명 댓글→ADMIN 승격→AUTHOR 초안 작성→ADMIN 발행→공개 상세에서 authorName 확인(소유권).
- e2e 회귀 수정: 비밀번호 표시 토글 버튼 도입 이후 `getByLabel('비밀번호')` 가 입력+버튼 2개에 매칭되어
  strict mode 위반으로 **기존 e2e 전부 실패**하던 상태였음 → `helpers.ts`/신규 spec 에서 `{ exact: true }` 로 한정.

## TDD 사이클 확인
- RED: `CommentForm.test`(로그인 시 입력 숨김/계정 이름 표시/displayName 미전송 3건), `CommentTree.test`(authorName 우선 2건) 먼저 추가 → 실패.
- GREEN: 컴포넌트 구현으로 통과. 단위 테스트 55개 pass (web vitest).
- REFACTOR: 표시용 CSS 분리, e2e 헬퍼 locator 한정.

## 결정한 것
- AUTHOR 는 글 작성/발행 API 권한은 있으나 대시보드 목록(GET /api/admin/posts)은 ADMIN 전용(ADR-0018, 기존 결정).
  따라서 e2e 의 "발행"은 ADMIN 이 작성자 초안을 발행하는 실제 운영 흐름으로 작성(새 ADR 불필요).
- 공개 화면에 로그아웃 UI 가 없어 역할별 세션은 Playwright 멀티 컨텍스트로 격리.

## 알려진 이슈
- 없음. (e2e 비밀번호 토글 locator 회귀는 본 태스크에서 해결)

## 다음 세션이 알아야 할 것
- multiuser-rbac 기능(S9.1~S9.4)의 마지막 태스크. feature_list.json 상 multiuser-rbac 전 태스크 done.
- e2e 에서 비밀번호 필드 선택 시 항상 `getByLabel('비밀번호', { exact: true })` 사용(토글 버튼과 충돌 방지).

## 명령 출력 증거
- `pnpm --filter web lint` → exit 0
- `pnpm --filter web typecheck` → exit 0
- `pnpm --filter web test` → Test Files 18 passed, Tests 55 passed
- `pnpm --filter web test:e2e` → 4 passed (12.7s) / dev DB 영향 없음
