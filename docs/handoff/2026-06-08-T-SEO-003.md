# Handoff — T-SEO-003 (SitemapService + /sitemap.xml + /robots.txt)

날짜: 2026-06-08 / 기능: seo-feed (E17) / 컨텍스트: Publishing

## 한 일
- 사이트맵·robots 를 TDD 로 추가했다(ADR-0026).
  - `seo/sitemap.service.ts`: `buildSitemap()` — 홈 + 발행글 슬러그 절대URL(lastmod=`updatedAt` YYYY-MM-DD) + 발행글 사용 태그(`/tags/:name`)를 sitemaps.org 0.9 urlset 으로. 발행글·태그를 `Promise.all` 2 findMany 로 배치 로드(N+1 없음). loc `escapeXml`. `buildRobots()` — `User-agent: * / Allow: / / Sitemap: {SITE_URL}/sitemap.xml`(절대).
  - `seo.controller.ts`: `@Get('sitemap.xml')`(application/xml), `@Get('robots.txt')`(text/plain) 추가 + Cache-Control.
  - `app-setup.ts`: exclude 에 sitemap.xml·robots.txt 추가(GET).
  - `publishing.module.ts`: SitemapService 등록.
- CLAUDE.md AUTO-MANAGED 표에 `/sitemap.xml`·`/robots.txt` 추가.

## TDD 사이클 확인
- **RED**: 모듈 부재로 실패(패턴 확립, 단위 spec 먼저).
- **GREEN**: 단위 3 통과, e2e `seo` 3 통과(feed·sitemap·robots).
- **REFACTOR**: `urlEntry` 헬퍼로 loc/lastmod 생성 일원화. e2e 에 초안 전용 태그 제외 단언 추가(code-reviewer Suggestion).

## 결정한 것
- 새 ADR 불필요(ADR-0026). lastmod 폴백(`updatedAt`→`publishedAt`)은 `@updatedAt` 가 NOT NULL 이라 도달 불가 → acceptance #2 문구를 "lastmod=updatedAt(@updatedAt NOT NULL) YYYY-MM-DD"로 정정(feature_list·tasks 동기).
- 태그 격리: 발행글에 사용된 태그만(`postTags.some.post.status PUBLISHED`) — 초안 전용 태그 비노출(e2e 검증).

## 알려진 이슈
- **라이브 dev 서버 stale dist(함정 #4)**: 컨테이너 nest 가 이전 dist 로 떠 있어 라이브 `curl /sitemap.xml` 은 404 일 수 있다. 코드는 무결하며 in-process e2e(configureApp+실제 HTTP)가 200·Content-Type·바디를 증명. **브라우저/curl 도그푸딩 전 api 재기동 필요**: `docker compose -f docker-compose.yml -f docker-compose.dev.yml restart api`(또는 watch 재빌드).

## 다음 세션이 알아야 할 것
- 다음 태스크: **T-SEO-004** (OgMetaService + `GET /og/posts/:slug`, deps=T-SEO-001 done). 봇 OG HTML — `og:*`/`twitter:card`·canonical, 미발행/없음 404, 외부이미지→기본이미지(`/og-default.png`) 폴백. `isExternalUrl` 화이트리스트 재확인(code-reviewer T-SEO-001 Suggestion 2). exclude 에 `og/(.*)` 추가(RequestMethod.ALL).
- 기본 OG 이미지 자산(og-default.png)은 T-SEO-006 에서 생성·배치 — T-SEO-004 폴백이 경로만 참조.

## 명령 출력 증거
- lint clean / tsc error 0
- 단위: `Test Suites: 24 passed, Tests: 162 passed`(seo 19)
- e2e: `Test Suites: 13 passed, Tests: 90 passed`(seo 3, 회귀 0)
- verifier: 5/5 PASS / code-reviewer: Critical·Warning 0(Suggestion 반영: 초안 태그 제외 e2e)
