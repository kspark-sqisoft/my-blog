# Handoff — T-WEB-001 (API 클라이언트 + 라우터 + Query/Tailwind)

- 날짜: 2026-06-02
- 태스크: T-WEB-001
- Context: WEB (E5 첫 태스크)
- status: done
- 의존: T-INFRA-003 (done)

## 한 일

- **web 테스트 하니스 구축**: vitest(jsdom) + @testing-library/jest-dom + setup 파일.
  - `vite.config.ts`: `test`(environment jsdom, globals, setupFiles), `@tailwindcss/vite` 플러그인,
    개발용 `/api → http://localhost:3000` 프록시.
  - `package.json`: `test`/`test:watch`/`typecheck` 스크립트.
- `src/lib/api.ts`: axios 인스턴스 `baseURL=/api`, `withCredentials=true`(ADR-0001 쿠키 동봉).
- `src/lib/queryClient.ts`: TanStack QueryClient.
- `src/routes.tsx` + `src/components/Placeholder.tsx`: TRD §5 라우트 7개(자리표시자).
- `src/main.tsx`: QueryClientProvider + RouterProvider(createBrowserRouter).
- `src/index.css`: `@import 'tailwindcss'` 추가(기존 테마 변수 유지).
- dead 스캐폴드 제거: `App.tsx`, `App.css`.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | axios baseURL=/api, withCredentials=true | ✅ 단위 테스트 |
| 2 | react-router TRD §5 라우트 등록 | ✅ 7경로 + 렌더 스모크 |
| 3 | Query Provider + Tailwind 동작 | ✅ 스모크 렌더 + vite build(CSS 7.77kB) |

게이트: lint 0, typecheck(tsc -b) 0, test 4/4, build green.

## 접속 방법 (프론트엔드)

- **로컬 개발**: 두 프로세스 띄우고 브라우저에서 **http://localhost:5173** 접속.
  - 백엔드: `DATABASE_URL=postgresql://blog:blog@localhost:5433/blog?schema=public pnpm --filter api start:dev` (포트 3000)
  - 프론트: `pnpm --filter web dev` (포트 5173) — `/api` 는 vite 프록시로 3000 으로 전달.
- **Docker 개발**: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up` → web 5173 / api 3000.
- 운영(빌드): web nginx가 정적 서빙 + `/api` 프록시(`packages/web/nginx.conf`).

## 다음 태스크가 알아야 할 것

- 페이지는 `Placeholder` 자리표시자다. T-WEB-002~007 에서 각 라우트 element 를 실제 페이지로 교체.
- 데이터 패칭은 `api`(axios) + TanStack Query 훅으로. 타입은 `@blog/shared`.
- 인증 UI 상태는 zustand(T-WEB-002), 폼은 react-hook-form + zod.
- 컴포넌트/데이터 혼재 파일은 react-refresh 린트 위반 → 컴포넌트는 별도 파일로.

## 변경 파일

- `packages/web/src/lib/{api,queryClient}.ts`, `src/routes.tsx`, `src/components/Placeholder.tsx`,
  `src/main.tsx`, `src/index.css`, `src/test/setup.ts`
- `src/lib/api.test.ts`, `src/routes.test.tsx`
- `packages/web/vite.config.ts`, `packages/web/package.json`
- 삭제: `src/App.tsx`, `src/App.css`
- `feature_list.json`, `docs/tasks/blog-mvp.md` (status done)
