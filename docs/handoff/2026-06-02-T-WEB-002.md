# Handoff — T-WEB-002 (로그인 페이지 + 보호 라우트)

- 날짜: 2026-06-02
- 태스크: T-WEB-002
- Context: WEB
- status: done
- 의존: T-WEB-001, T-AUTH-004 (done)

## 한 일

- `src/auth/useAuth.ts` — zustand 인증 스토어: `user/status/error` + `login/fetchMe/logout`.
  - httpOnly 쿠키 기반이라 토큰은 보관하지 않음(ADR-0001). 실패 시 단일 메시지.
- `src/pages/Login.tsx` — react-hook-form + zod 폼. 제출 시 `login()` → 성공하면 `navigate('/admin')`, 실패 시 `role="alert"` 메시지.
- `src/auth/ProtectedRoute.tsx` — 마운트 시 `fetchMe()`(GET /auth/me), 미인증이면 `<Navigate to="/login" replace />`, 확인 중엔 로딩.
- `src/routes.tsx` — `/login`→Login, `/admin*`를 `<ProtectedRoute>` 로 감쌈(flat 경로 유지).

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | 폼 제출 → POST /auth/login, 성공 시 /admin | ✅ (api 모킹, ADMIN PAGE 렌더 확인) |
| 2 | me 세션 확인, 미인증 시 /login 리다이렉트 | ✅ |
| 3 | 인증 실패 메시지 | ✅ role=alert |

게이트: lint 0, typecheck 0, test 8/8, build green.

## 메모

- 테스트는 `vi.mock('../lib/api')` 로 axios 인스턴스를 모킹, zustand 상태는 `beforeEach` 에서 리셋.
- T-WEB-001 routes 스모크의 `/login` 단언을 placeholder→로그인 버튼으로 갱신(정당한 교체).
- 보호는 children 래퍼 방식(`<ProtectedRoute>{el}</ProtectedRoute>`) — 라우트 경로 평면 유지로 기존 테스트 보존.

## 다음 태스크가 알아야 할 것

- T-WEB-003(목록)·004(상세)는 `api`+TanStack Query 훅으로 데이터 패칭. 타입 `@blog/shared`.
- 로그아웃/네비게이션 헤더는 아직 없음(대시보드 T-WEB-006에서). useAuth.logout 사용 가능.
- 실제 로그인 동작 확인은 백엔드 기동 필요(운영자 시드 후): `pnpm --filter api db:seed`.

## 변경 파일

- `packages/web/src/auth/{useAuth.ts,ProtectedRoute.tsx,ProtectedRoute.test.tsx}`
- `packages/web/src/pages/{Login.tsx,Login.test.tsx}`
- `packages/web/src/routes.tsx`, `src/routes.test.tsx`
- `feature_list.json`, `docs/tasks/blog-mvp.md` (status done)
