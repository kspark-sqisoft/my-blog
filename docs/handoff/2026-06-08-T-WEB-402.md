# Handoff — T-WEB-402 (작성자 프로필 페이지 /users/:id + 작성자 링크)

## 한 일
- 공개 작성자 프로필 페이지와 작성자 이름 링크를 web 에 추가 (author-profile, ADR-0028). API 토대(T-AUTH-013 `GET /api/users/:id`, T-PUB-107 `?author`)를 web 에서 조립.
- 신규/변경 파일:
  - `packages/web/src/pages/AuthorProfile.tsx`(신규) — `/users/:id` 페이지. `useAuthorProfile(id)`(헤더) + `usePosts({ author: id })`(목록). 헤더: Avatar(lg)·이름(h1)·bio(있을 때)·가입일(fmtDate)·발행한 글 N개. `PostListView` 에 `emptyText="아직 발행한 글이 없습니다."`. 로딩/에러(작성자 없음) 상태.
  - `packages/web/src/profile/useAuthorProfile.ts`(신규) — `GET /api/users/:id` useQuery 훅(queryKey `['author', id]`, enabled).
  - `packages/web/src/posts/usePosts.ts` — `PostsQuery.author?: string` 추가(쿼리 params 로 그대로 전달).
  - `packages/web/src/components/PostListView.tsx` — 작성자 이름을 `<Link to={/users/:authorId}>` 로 감쌈(이름 텍스트만 → 접근가능 이름 1개). `emptyText` optional prop(기본 "아직 글이 없습니다." — 기존 동작 무변경).
  - `packages/web/src/pages/PostDetail.tsx` — 상세 헤더 작성자 이름을 동일 링크로.
  - `packages/web/src/routes.tsx` — PublicLayout 하위에 `/users/:id` 등록.
  - 테스트: `AuthorProfile.test.tsx`(신규 5건), `PostDetail.test.tsx`·`PostList.test.tsx`(작성자 링크 회귀 검증 추가, mock 에 authorId 보강).

## TDD 사이클 확인
- **RED**: AuthorProfile.test(헤더·author 파라미터·빈목록·카드 링크) + PostDetail 링크 검증 추가 → AuthorProfile import 실패 + PostDetail 링크 테스트 실패.
- **GREEN**: usePosts.author / useAuthorProfile / AuthorProfile / 라우트 / PostListView·PostDetail 링크 구현 → 143 통과.
- **REFACTOR**: code-reviewer Warning/Suggestion 반영 — PostList.test mock 에 authorId 추가 + 링크 role 검증으로 강화, AuthorProfile.test 에 가입일·bio-null 케이스 추가 → 144 통과.

## 결정한 것
- 빈 목록 문구가 작성자 프로필에선 "아직 발행한 글이 없습니다."(홈/태그는 "아직 글이 없습니다.") → `PostListView` 에 optional `emptyText` prop 추가(기본값으로 기존 동작 보존, 회귀 0). 새 ADR 불필요.
- 작성자 링크는 이름 텍스트만 감싼다(아바타는 비감쌈) → 작성자당 접근가능 링크 1개로 중복 방지(접근성).
- 데이터는 전부 훅 경유(useAuthorProfile/usePosts), 컴포넌트 직접 fetch 0(web 절대규칙). 폼 없음 → zod/shared 순수성 무관.

## 알려진 이슈
- 없음. web 단위 144/144, typecheck·lint 클린.
- Playwright e2e(BDD)는 이번 추가 안 함(acceptance 는 단위+회귀 0). 후속 백로그: `/users/:id` 여정 e2e(GWT).

## 다음 세션이 알아야 할 것
- author-profile 마지막 태스크: **T-WEB-403**(`/profile` 소개 bio textarea, `useUpdateProfile` 확장 — 이번 PATCH bio(T-AUTH-014) 소비). deps T-AUTH-014(done). 이걸 끝내면 author-profile(E19) 기능 완결.
- web 폼은 인라인 zod(shared 금지, 함정 #9). `useUpdateProfile` 은 이미 `UpdateProfileDto`(bio 포함)를 받으므로 bio 추가 시 별도 훅 변경 최소.

## 명령 출력 증거
- lint: `eslint .` 클린. typecheck: `tsc -b --noEmit` 클린.
- 단위: `pnpm --filter web test` → **Test Files 32 passed / Tests 144 passed**.
- verifier: acceptance 4/4 **PASS**. code-reviewer: Critical 0 / Warning 1(반영: PostList.test authorId) / Suggestion 3(반영: 가입일·bio-null 검증, 링크 role 강화).

평가: Accept (12/12)
