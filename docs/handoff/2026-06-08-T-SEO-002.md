# Handoff — T-SEO-002 (FeedService + GET /feed.xml)

날짜: 2026-06-08 / 기능: seo-feed (E17) / 컨텍스트: Publishing

## 한 일
- RSS 2.0 피드를 TDD 로 추가했다(ADR-0026).
  - `packages/api/src/publishing/seo/feed.service.ts`: 발행글만 `publishedAt` 최신순·최대 `FEED_MAX_ITEMS`(기본 20) 단일 `findMany`+`include(author)` 로 배치 로드(N+1 없음) → RSS 2.0 XML 문자열 생성. item 은 제목·요약(`toSummaryText`)·link/guid(슬러그 절대 URL)·`pubDate`(RFC822, `toUTCString`)·`dc:creator`(표시 이름). 모든 사용자 콘텐츠 `escapeXml`.
  - `packages/api/src/publishing/seo/seo.controller.ts`: `@Get('feed.xml')` + `@Header`(Content-Type `application/rss+xml; charset=utf-8`, `Cache-Control: public, max-age=300`). 글로벌 응답 래핑 인터셉터가 없어 문자열이 그대로 raw XML 로 나간다.
  - `packages/api/src/publishing/seo/site-config.ts`: `SITE_NAME`·`SITE_DESCRIPTION`·`feedMaxItems()`(OG 도 재사용 예정).
  - `app-setup.ts`: `setGlobalPrefix('api', { exclude: [{ path: 'feed.xml', method: GET }] })` — 크롤러 표준 루트 경로 노출. 기존 `/api/*` 라우트 영향 0(e2e 88 회귀 없음).
  - `publishing.module.ts`: SeoController·FeedService 등록.
- CLAUDE.md AUTO-MANAGED 표에 `GET /feed.xml` 추가.

## TDD 사이클 확인
- **RED**: `test -- src/publishing/seo/feed` → `Cannot find module './feed.service'`.
- **GREEN**: 구현 후 단위 5 통과. e2e `test:e2e -- seo` 1 통과.
- **REFACTOR**: spec mock 을 `jest.fn<Promise<unknown[]>,[FindManyArg]>()` 제네릭으로 타입 안전화(formatter 가 `as never` 제거하며 생긴 tsc/lint unsafe 해소).

## 결정한 것
- 새 ADR 불필요(ADR-0026). `@Header` raw 문자열 반환 = 현 구조(글로벌 인터셉터 없음)에 적합.
- 작성자: RSS `author`(이메일·PII) 대신 `dc:creator`(이름). `xmlns:dc` 선언.

## 알려진 이슈
- 피드는 `<category>`(태그)를 내보내지 않는다 — 추후 카테고리 추가 시 태그 배치 로드 별도 필요(현 범위 무관).
- docker-compose SITE_URL/FEED_MAX_ITEMS 는 dev 컨테이너 재빌드 시 반영(e2e 는 jest 가 앱 부팅해 무관). 브라우저 도그푸딩 시 재빌드 안내.

## 다음 세션이 알아야 할 것
- 다음 태스크: **T-SEO-003** (SitemapService + `GET /sitemap.xml` + `GET /robots.txt`, deps=T-SEO-001 done). `absoluteUrl`/`escapeXml`·`siteUrl` 재사용, `app-setup.ts` exclude 에 `sitemap.xml`·`robots.txt` 추가, lastmod=`updatedAt`, 태그 페이지(`/tags/:name`)·홈 포함, 초안 제외.
- seo 산출물 컨트롤러는 SeoController 에 메서드 추가로 확장(모듈 재등록 불필요).

## 명령 출력 증거
- lint clean / tsc `--noEmit` error 0
- 단위: `Test Suites: 23 passed, Tests: 159 passed`(seo 16)
- e2e: `Test Suites: 13 passed, Tests: 88 passed`(seo 1, 회귀 0)
- verifier: acceptance 5/5 PASS / code-reviewer: Critical·Warning 0(Suggestion 반영: e2e publish .expect(200))
