# Handoff — T-PUB-008 (운영자 Post 목록 GET /api/admin/posts) + 테스트 DB 분리

- 날짜: 2026-06-02
- 태스크: T-PUB-008 (T-WEB-006 선행으로 추가 등록)
- Context: PUBLISHING
- status: done
- 의존: T-PUB-002, T-AUTH-003 (done)

## 한 일

- `@blog/shared`: `AdminPostSummaryDto { id, title, status, tags, publishedAt, createdAt }`.
- `PostService.listForAdmin({page,pageSize})` → 초안+발행 전체, `createdAt desc`, status 포함, 페이지네이션(`$transaction`).
- `AdminPostController`(`@Controller('admin/posts')`, `@UseGuards(JwtAuthGuard)`) `GET /api/admin/posts`.
- PublishingModule 에 등록.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | JwtAuthGuard 미인증 401 | ✅ |
| 2 | 초안+발행 status 포함 최신순 + 페이지네이션 | ✅ |
| 3 | AdminPostSummaryDto 형태 | ✅ |

게이트: lint 0, tsc 0, unit 43/43, e2e 31/31.

## 곁들여 해결한 인프라 이슈 (중요)

1. **테스트 DB 분리(blog_test)** — 통합 테스트가 개발 DB(blog)를 공유해 전역 카운트가 깨지고
   개발 데이터를 지우던 문제. jest setup(`src/test-db.setup.ts`, `test/jest-e2e.setup.ts`)이
   `DATABASE_URL` 을 `blog_test` 로 강제. `init.sh` 가 blog_test 생성+마이그레이션 자동화.
   → docs/harness.md "운영 함정 #3", CLAUDE.md 절대규칙 #8 에 기록.
2. **localhost→IPv6(::1) 이슈** — Prisma CLI(migrate/generate)가 `localhost` 로는 P1001.
   `init.sh`·테스트 setup 의 DB URL 을 `127.0.0.1` 로 고정(테스트 pg 어댑터는 영향 없지만 통일).

## 다음 태스크가 알아야 할 것 (T-WEB-006)

- 대시보드는 `GET /api/admin/posts`(쿠키 인증)로 초안+발행 목록을 가져온다 → `AdminPostSummaryDto[]`.
- 발행/취소/삭제는 기존 `POST /posts/:id/publish|unpublish`, `DELETE /posts/:id`.
- ProtectedRoute 안에서 렌더(이미 /admin 보호됨).

## 변경 파일

- `packages/shared/src/dto/post.ts`, `src/index.ts`
- `packages/api/src/publishing/{post.service.ts,admin-post.controller.ts,publishing.module.ts}`
- `packages/api/src/publishing/post.service.spec.ts`, `test/admin-posts.e2e-spec.ts`
- 테스트 DB: `src/test-db.setup.ts`(신규), `test/jest-e2e.setup.ts`, `package.json`(jest setupFiles)
- `init.sh`(blog_test + 127.0.0.1), `docs/harness.md`, `CLAUDE.md`
- `feature_list.json`, `docs/tasks/blog-mvp.md`
