# Handoff — T-INFRA-003 (공유 타입 패키지)

- 날짜: 2026-06-02
- 태스크: T-INFRA-003 — 공유 타입 패키지 스캐폴드 (packages/shared)
- Context: INFRA
- status: done
- 의존: 없음

## 한 일

- `packages/shared/src/dto/*.ts`에 TRD §6 타입 계약 작성, `index.ts`에서 `export type`로 재export.
- `tsconfig.json` + `package.json`(build/typecheck 스크립트, `exports`/`types`).
- ADR-0004 정합: 미사용 `zod` 의존 제거(shared는 순수 타입), `typescript` devDep 추가.
- **api·web에 `@blog/shared: workspace:*` 의존성 추가** (acceptance #3: import 해석 위해 필수).

## 정의한 타입 (패키지명: `@blog/shared`)

| 타입 | 파일 |
|---|---|
| `PostStatus`, `PostSummaryDto`, `PostDetailDto`, `CreatePostDto`, `UpdatePostDto` | dto/post.ts |
| `CommentDto`(depth+replies 중첩), `CreateCommentDto` | dto/comment.ts |
| `TagDto` | dto/tag.ts |
| `AuthUserDto` | dto/auth.ts |
| `UploadResultDto` | dto/upload.ts |
| `Paginated<T>` | dto/pagination.ts |

날짜 필드는 JSON 직렬화 기준 ISO 8601 `string`. `publishedAt`은 `string | null`.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | TRD §6 타입 계약 export | ✅ 10개 타입 export (api/web에서 전부 import 확인) |
| 2 | build/typecheck 통과 | ✅ `pnpm --filter @blog/shared build` & `typecheck` exit 0 |
| 3 | api/web에서 import 해석 | ✅ api(nodenext)·web(bundler) 임시 import 파일 tsc exit 0 |

검증 메모: `node require.resolve`는 main이 `.ts`라 부적합 → TS 프로젝트 tsc로 검증.
임시 import 체크 파일은 검증 후 삭제함. `dist/`는 .gitignore 처리(커밋 제외).

## 다음 태스크가 알아야 할 것

- 모든 DTO는 `import type { X } from '@blog/shared'`로 사용.
- 검증은 각 패키지가 담당(ADR-0004): api=class-validator DTO, web=zod 스키마. shared에는 검증 로직 없음.
- `verbatimModuleSyntax: true`라 shared에서 값(value) export 금지 — 타입만.

## 변경 파일

- `packages/shared/src/index.ts`, `packages/shared/src/dto/{post,comment,tag,auth,upload,pagination}.ts` (신규)
- `packages/shared/tsconfig.json` (신규)
- `packages/shared/package.json` (build/typecheck/exports, zod 제거)
- `packages/api/package.json`, `packages/web/package.json` (@blog/shared 의존성)
- `pnpm-lock.yaml`
- `docs/tasks/blog-mvp.md` (status done)
