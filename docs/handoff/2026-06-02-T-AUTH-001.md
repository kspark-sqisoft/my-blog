# Handoff — T-AUTH-001 (User 시드 스크립트)

- 날짜: 2026-06-02
- 태스크: T-AUTH-001 — User 시드 스크립트 (환경변수 부트스트랩, bcrypt)
- Context: AUTH
- status: done
- 의존: T-INFRA-002 (done)
- 관련: ADR-0002

## 한 일

- `src/auth/seed-operator.ts` — `seedOperator(prisma, { email, password })`:
  bcrypt 해시(SALT_ROUNDS=10) 후 email 기준 `upsert`(update 비움 → 멱등).
- `prisma/seed.ts` — OPERATOR_EMAIL/PASSWORD 읽어 PrismaClient(어댑터)로 seedOperator 실행.
- `db:seed` 스크립트(`tsx prisma/seed.ts`) 추가, `tsx` devDep.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | OPERATOR_*로 User 생성, bcrypt 해시(평문 미저장) | ✅ `bcrypt.compare` true, passwordHash≠평문 |
| 2 | 재실행 시 중복 생성 안 함(upsert) | ✅ spec count=1 + 실DB 2회 실행 후 1건 |
| 3 | 시드 후 User 정확히 1건 | ✅ |

게이트: lint 0, tsc 0, unit 7/7, e2e 5/5.
end-to-end: `pnpm --filter api db:seed` 2회 → `users` 1건 확인.

## 다음 태스크가 알아야 할 것

- 운영자 자격 검증은 `bcrypt.compare(평문, user.passwordHash)` (T-AUTH-002 AuthService).
- 시드 실행 전제: db 기동 + `DATABASE_URL`, `OPERATOR_EMAIL`, `OPERATOR_PASSWORD`.
  (`.env.example`에 키 존재. 로컬 DATABASE_URL=localhost:5433)
- `seedOperator`는 `Pick<PrismaClient,'user'>`만 의존 → PrismaService도 그대로 전달 가능.
- 통합 spec은 `beforeEach`에서 `user.deleteMany()`로 격리 → 테스트 실행 시 시드 계정은 지워짐(정상).
- bcrypt 네이티브 모듈은 현재 환경에서 정상 동작(빌드됨).

## 변경 파일

- `packages/api/src/auth/seed-operator.ts` (신규)
- `packages/api/src/auth/seed-operator.spec.ts` (신규)
- `packages/api/prisma/seed.ts` (신규)
- `packages/api/package.json` (db:seed 스크립트, tsx devDep)
- `pnpm-lock.yaml`
- `docs/tasks/blog-mvp.md` (status done)
