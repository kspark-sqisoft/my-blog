# Handoff — T-AUTH-010 (사용자 관리 API) · Phase 2 완료

날짜: 2026-06-04 / 기능: multiuser-rbac (Phase 2)

## 한 일
- `user-admin.service.ts`: `list()`(Paginated<AdminUserDto>, createdAt desc), `updateRole()`(트랜잭션 내 마지막 ADMIN 강등 차단 409, 없는 사용자 404).
- `user-admin.controller.ts`: `@UseGuards(JwtAuthGuard, RolesGuard) @Roles('ADMIN')`, `GET /api/admin/users`, `PATCH /api/admin/users/:id/role`.
- DTO: `dto/users.query.ts`(page/pageSize), `dto/update-user-role.dto.ts`(@IsIn 역할 검증).
- `auth.module.ts`: controller·service 등록.
- `CLAUDE.md` 엔드포인트 표에 admin/users 2건 추가.
- admin-users.e2e 7케이스: 401/403/200 목록/승격 200/잘못된 role 400/마지막 ADMIN 409/2명일 때 강등 200.

## 검증
- api 유닛 69 / e2e 51(+7) / typecheck / lint 통과.

## Phase 2 완료
- T-AUTH-009(회원가입)·T-AUTH-010(사용자 관리) done. 백엔드 회원가입+역할관리 완성.

## 다음 (Phase 3)
- T-CONV-004: 댓글 회원 연동(OptionalJwtAuthGuard + userId/실명 + CommentDto authorName).
