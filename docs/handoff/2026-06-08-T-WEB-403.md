# Handoff — T-WEB-403 (/profile 소개(bio) 편집) · author-profile(E19) 완결

## 한 일
- `/profile` 프로필 폼에 소개(bio) 편집 textarea 추가 (author-profile, ADR-0028). T-AUTH-014 의 `PATCH /api/auth/me` bio 를 web 에서 소비.
- 변경 파일:
  - `packages/web/src/pages/Profile.tsx`:
    - `profileSchema` 에 `bio: z.string().max(200, '소개는 200자 이하여야 합니다')` (인라인 zod, shared 순수성 함정 #9 준수).
    - `defaultValues.bio = user?.bio ?? ''` (현재 bio 로드).
    - 이름 필드 아래 `<textarea aria-label="소개" maxLength={200}>` + `{bioLength}/200` 카운터.
    - 카운터는 `register('bio').onChange` 에서 `setBioLength` 로 갱신(`watch` 미사용 — `react-hooks/incompatible-library` 경고 회피). `setValue('bio')` 추가 시 동기화 주석.
  - `packages/web/src/pages/Profile.test.tsx`: bio 테스트 5건(로드 / 입력+저장 PATCH bio / 빈 bio 저장 bio:'' / 201자 에러+미저장 / 200자 제한 표시) + 기존 name 저장 테스트 payload 에 `bio:''` 반영.
- `useUpdateProfile` 은 이미 `UpdateProfileDto`(bio 포함) 전송 → 별도 훅 변경 불필요.

## TDD 사이클 확인
- **RED**: Profile.test 에 bio 5건 추가 + 기존 name payload 에 bio:'' → 5건 실패(bio 미구현).
- **GREEN**: schema.bio + textarea + 카운터 구현 → 148 통과.
- **REFACTOR**: 초기 `watch('bio')` 가 lint 경고(`react-hooks/incompatible-library`) → state 기반 카운터로 교체(경고 0). code-reviewer Warning 반영: 빈 bio 저장 payload 테스트 추가(149), setValue 동기화 주석.

## 결정한 것
- bio 카운터는 `watch` 대신 onChange→state. RHF watch 와 조기 return(`if (!user) return null`) 조합이 React Compiler 호환성 경고를 유발 → state 로 회피.
- 빈 문자열 `''` 전송 = 소개 제거. 공개 프로필(AuthorProfile)은 `{author.bio && ...}` 로 렌더 → `''`·`null` 모두 미표시(일관). 서버는 `''` 그대로 저장(null 정규화 없음) — 표시 끝단 일관하므로 추가 처리 불필요. 새 ADR 불필요.
- 폼 zod 는 Profile.tsx 인라인(ADR-0004·함정 #9). shared 에 zod 의존/스키마 없음(검증됨).

## 알려진 이슈
- 없음. web 단위 149/149, typecheck·lint 클린.
- code-reviewer Suggestion(선택, 미반영): 카운터 `aria-live` 미부여(접근성 강화), maxLength 하드컷과 zod 에러의 의도 일관화. 기능 영향 없음.

## 다음 세션이 알아야 할 것
- **author-profile(E19) 기능 완결**: T-AUTH-012 / T-PUB-107 / T-AUTH-013 / T-AUTH-014 / T-WEB-402 / T-WEB-403 전부 done. `feature_list.json` E19 항목 모두 done.
- **다음 기능: `series`(ADR-0029) — 연재/시리즈, Publishing 신규 Aggregate.** 6-feature 로드맵의 4번째. 새 세션은 10-Phase(PRD→BC→TRD→ADR→tasks→TDD)로 시작. 그 뒤 bookmarks(ADR-0030), notifications(ADR-0031).
- author-profile 백로그: `/users/:id` 여정 Playwright e2e(GWT), 카운터 aria-live.

## 명령 출력 증거
- lint: `eslint .` 경고/에러 0(초기 watch 경고 1 → state 교체 후 0). typecheck: `tsc -b --noEmit` 에러 0.
- 단위: `pnpm --filter web test` → **Test Files 32 passed / Tests 149 passed**.
- verifier: acceptance 3/3 **PASS**. code-reviewer: Critical 0 / Warning 3(반영: 빈 bio 테스트·동기화 주석, 확인: ''·null 렌더 일관) / Suggestion 3(선택).

평가: Accept (12/12)
