# Handoff — T-CONV-007 (CommentService.remove 조건부 soft/hard + 답글 차단)

날짜: 2026-06-08 / 기능: comment-moderation (E18) / 컨텍스트: Conversation

## 한 일
- 댓글 삭제(조건부)·소프트 부모 답글 차단을 TDD 로 구현했다(ADR-0027).
  - `comment.service.ts`:
    - `remove(id, actor)`: `requireComment`(404) → `assertCanDelete`(403) → 직계 답글 `count > 0` 면 soft(`deletedAt=now`, 노드·답글 보존), 없으면 hard(`delete`).
    - `assertCanDelete`: 본인(`userId===actor.id`) ‖ ADMIN ‖ 글쓴이(`Post.authorId` — `findUnique select authorId` **read-only** 1회). 익명(userId null)은 본인 미성립 → ADMIN/글쓴이만.
    - `create`: `parentId` 부모 `deletedAt != null` 이면 `BadRequestException`(400, S2).
  - `comment.service.spec.ts`: remove 5(hard/soft/403/글쓴이+spy 1회/404) + create 차단 1.
  - `comment.service.depth.spec.ts`: mock parentRow 에 `deletedAt: null` 추가(create 차단 select 반영 — 회귀 해소).

## TDD 사이클
- **RED**: remove 미구현·차단 미적용 → fail.
- **GREEN**: 구현 후 comment.service.spec 19 통과. 단, depth.spec 3건이 mock 의 deletedAt 누락(`undefined !== null` → 오차단)으로 실패 → mock 보강 후 전체 181 통과.
- **REFACTOR/검증보강**: verifier #3 FAIL("호출 1회 단언" 테스트 부재) → `jest.spyOn(prisma.post,'findUnique')` + `toHaveBeenCalledTimes(1)` 단언 추가 → 5/5 PASS.

## 결정한 것
- 새 ADR 불필요(ADR-0027). 조건부 삭제는 `count({where:{parentId}})` 단일 쿼리로 soft/hard 분기.
- 글쓴이 권한은 `Post.authorId` read-only ID 참조(BC 정합, 트랜잭션 내 Post 미수정).

## 알려진 이슈 / 후속 (백로그)
- **[후속] `Comment.parentId` 인덱스 부재**: `remove` 의 `count({where:{parentId}})`·답글 트리 조회가 `parentId` 인덱스 없이 동작(현재 `@@index([postId])`·`@@index([userId])`만). 삭제는 드문 쓰기 경로라 현 규모 문제 없으나, 댓글량 증가 시 `@@index([parentId])` 추가 마이그레이션 권장(code-reviewer Suggestion, 본 태스크 범위 밖).
- `comment.service.spec` 상단 `DATABASE_URL ??= ...blog` 는 기존 줄(내가 추가 안 함). test-db.setup 이 blog_test 강제 override(verifier 확인).

## 다음 세션이 알아야 할 것
- 다음: **T-CONV-008** (CommentController PATCH/DELETE + 가드 + e2e, deps=007 done). `JwtAuthGuard`+`ThrottlerGuard`, actor=`req.user{id,role}`(JwtStrategy DB 재조회 — `actorOf(req)` 패턴), `UpdateCommentDto` class-validator(body 1~maxLen, CreateCommentDto 상수 재사용). e2e 권한 매트릭스(401→404→403, soft/hard 왕복, 익명 운영자 삭제, soft 부모 답글 400). 신규 e2e spec 은 DATABASE_URL 자체 기본값 금지(#8).
- 그 다음 T-WEB-401(웹 UI).
- service 의 `update`/`remove` 는 완성 — 008 은 컨트롤러 배선 + e2e 만.

## 명령 출력 증거
- comment.service.spec: 19 passed / 전체 단위: 181 passed
- api tsc 0 / lint clean / shared 무변경
- verifier: 5/5 PASS(#3 spy 단언 추가 후) / code-reviewer: Critical·Warning 0(Suggestion: parentId 인덱스 후속)
