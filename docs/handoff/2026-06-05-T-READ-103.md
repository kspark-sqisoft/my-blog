# Handoff — T-READ-103 (TOC 사이드바 + 스크롤스파이)

읽기 경험(B) 세 번째 태스크. T-READ-102 가 추출한 목차(toc)를 사이드바로 표시.

## 한 일
- `packages/web/src/components/ArticleToc.tsx` — 목차 컴포넌트 신설.
  - `toc[]` 를 `<details>`(모바일 접이식) + `<nav><ol>` 로 렌더, 레벨별 들여쓰기 클래스(`lv1~3`).
  - IntersectionObserver 로 화면에 보이는 헤딩을 추적 → 활성 항목 `aria-current="location"` + `.active`.
  - 항목이 없으면 `null` 반환(목차 숨김). 언마운트 시 `observer.disconnect()`.
- `packages/web/src/pages/PostDetail.tsx` — `ReadingArticle onToc={setToc}` 로 목차 수신,
  본문+TOC 2단 그리드(`ab-reading-main`). 목차 있으면 `ab-reading--with-toc`(폭 확장),
  데스크톱은 사이드바 우측 sticky, 없으면 단일 컬럼 풀폭.
- `packages/web/src/index.css` — TOC 사이드바/그리드/활성 스타일 + 헤딩 `scroll-margin-top`(앵커 가림 방지).
- `packages/web/src/test/setup.ts` — jsdom 에 없는 `IntersectionObserver` no-op 스텁 추가
  (ArticleToc 를 렌더하는 다른 테스트의 크래시 방지. 동작 검증 테스트는 vi.stubGlobal 로 덮어씀).

## TDD 사이클 확인
- RED: `ArticleToc.test.tsx`(4) 작성 → 모듈 부재로 import 실패.
- GREEN: `ArticleToc.tsx` 구현 → 스크롤스파이 테스트가 act() 누락으로 1건 실패 → 테스트에 act() 보강 → 115 통과.
- REFACTOR/통합: PostDetail 2단 그리드 연결 후 기존 `PostDetail.test.tsx` 2건이
  `IntersectionObserver is not defined` 로 실패 → setup 에 전역 스텁 추가 → 26파일/115 통과.

## 결정한 것
- 모바일 접이식 + 데스크톱 sticky 를 단일 `<details>` 로 처리(데스크톱은 summary `pointer-events:none` 로 항상 펼침).
- 활성 항목 판정: 가시 헤딩 중 목차 순서상 먼저인 것(jsdom 에서도 결정적, 테스트 가능).
- IntersectionObserver 전역 스텁은 setup 에 둠(컴포넌트가 환경 가드를 갖지 않고 깔끔하게 유지).

## 알려진 이슈
- 매우 긴 글에서 sticky 목차가 뷰포트보다 길면 `overflow-y:auto` 로 자체 스크롤(의도).

## 다음 세션이 알아야 할 것
- 다음 태스크: **T-READ-104**(관련 글 API + shared 타입 + web 섹션). 의존 T-WEB-309(충족).
  - shared `RelatedPostDto`, `GET /api/posts/:idOrSlug/related?limit=4`(태그 겹침 desc → publishedAt desc, 자기 제외, 부족분 최신 보완), web `useRelatedPosts` + `RelatedPosts` 섹션.

## 명령 출력 증거
- lint/typecheck: 무오류.
- unit: `Test Files 26 passed (26)`, `Tests 115 passed (115)`.
- e2e: `pnpm test:e2e`(격리) — `7 passed (23.3s)`, dev DB 무영향(Post:Comment 32:99 불변).
