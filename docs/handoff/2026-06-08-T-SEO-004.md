# Handoff — T-SEO-004 (OgMetaService + GET /og/posts/:slug)

날짜: 2026-06-08 / 기능: seo-feed (E17) / 컨텍스트: Publishing

## 한 일
- 봇용 Open Graph 메타 HTML 을 TDD 로 추가했다(ADR-0026 핵심 결정).
  - `seo/og-meta.service.ts`: slug/cuid 로 **발행 Post 만** 조회 → `og:type=article`·`og:site_name`·`og:title`·`og:description`(요약)·`og:url`(canonical 절대)·`og:image`(절대)·`twitter:card/title/description/image`·`<link rel=canonical>` 채운 최소 HTML(+`<noscript>` 요약). 모든 값 `escapeXml`. 미발행/없음 → `NotFoundException`(404).
  - **og:image 화이트리스트**: 대표 이미지가 `/uploads/` 로 시작(+`..` 미포함)할 때만 신뢰해 절대화, 외부 절대URL·protocol-relative(`//host`)·없음은 기본 이미지(`/og-default.png`) 폴백. 외부 URL fetch 안 함 → SSRF·외부 추적·깨진 카드 차단. (T-SEO-001 code-reviewer Suggestion 2 이행)
  - `seo/og.controller.ts`: `@Controller('og')` + `@Get('posts/:slug')`(text/html). `app-setup.ts` exclude 에 `og/(.*)` 추가.
  - `site-config.ts`: `DEFAULT_OG_IMAGE='/og-default.png'`.
  - `publishing.module.ts`: OgController·OgMetaService 등록.
- CLAUDE.md AUTO-MANAGED 표에 `/og/posts/:slug` 추가.

## TDD 사이클 확인
- **RED**: 모듈 부재 실패.
- **GREEN**: 단위 8(uploads 절대화·외부 폴백·protocol-relative 폴백·없음 폴백·이스케이프·404·발행격리), e2e 4(feed·sitemap·robots·og).
- **REFACTOR**: ogImage 를 isExternalUrl→화이트리스트(`/uploads/` + `..` 미포함)로 강화. `isExternalUrl` import 제거.

## 결정한 것
- 새 ADR 불필요(ADR-0026). og:image 신뢰 기준을 "외부면 폴백"에서 **"로컬 /uploads 만 신뢰"** 화이트리스트로 강화(우회 표면 축소).

## 알려진 이슈
- 라이브 dev 서버 stale dist(함정 #4) — 봇 OG 라이브 검증은 api 재기동 후 `curl -A "Twitterbot" http://localhost:5173/og/posts/:slug` (또는 e2e 가 in-process 증명).
- 빈 본문 글은 og:description 이 빈 문자열(카드 품질만 영향, 동작 정상) — 필요 시 후속에서 SITE_DESCRIPTION 폴백.

## 다음 세션이 알아야 할 것
- 다음 태스크: **T-SEO-005** (nginx 봇 UA 분기 + 산출물 프록시 + vite proxy, deps=002·003·004 done). `packages/web/nginx.conf` 에 `map $http_user_agent $is_crawler` + `location ~ ^/posts/`(봇→api `/og/posts/...`, 사람→`try_files index.html`) + `= /feed.xml`·`= /sitemap.xml`·`= /robots.txt`·`/og/` 프록시, `vite.config.ts` proxy 동일 경로. nginx `-t` + 봇/사람 스모크.
- 그 다음 **T-SEO-006** (web index.html 기본 OG + feed link + `public/og-default.png` 사이트명 PNG 생성).
- seo api 4 엔드포인트(feed/sitemap/robots/og) 완성 — 남은 건 서빙 정합(WEB)뿐.

## 명령 출력 증거
- lint clean / tsc error 0
- 단위: seo 27 통과(og 8), 전체 169
- e2e: seo 4 통과(og 포함), 전체 91(회귀 0)
- verifier: 5/5 PASS / code-reviewer: Critical·Warning 0(Suggestion 반영: traversal 심층방어)
