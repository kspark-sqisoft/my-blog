# Handoff — T-PUB-007 (TagController: GET /api/tags)

- 날짜: 2026-06-02
- 태스크: T-PUB-007
- Context: PUBLISHING
- status: done
- 의존: T-PUB-003, T-PUB-006 (done)

## 한 일

- `TagService.listUsedTags()` → `TagDto[]`:
  - 발행(PUBLISHED) Post에 연결된 PostTag를 조회해 name별 postCount 집계(JS), name 정렬.
  - 미사용/초안 전용 태그 제외. (PrismaService 주입 추가)
- `TagController`(`/api/tags`, 공개) `GET /` → listUsedTags.
- PublishingModule controllers에 TagController 추가.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | 발행 Post 사용 태그 + postCount | ✅ x→2, y→1 |
| 2 | 미사용/초안 전용 태그 제외 | ✅ z 없음 |
| 3 | 공개 접근 | ✅ 인증 없이 200 |

게이트: lint 0, tsc 0, unit 31/31, e2e 20/20.

## E2(Publishing) 완료

T-PUB-001~007 모두 done. Post CRUD·발행·상세·목록·Tag·업로드(미구현, E3)·컨트롤러 완비.
실제 라우트: `GET/POST/PATCH/DELETE /api/posts`, `POST /api/posts/:id/(un)publish`, `GET /api/tags`.

## 다음 단계

- **E3 업로드**: T-PUB-101(StorageProvider+Local), T-PUB-102(POST /api/uploads). PublishingModule에 UploadController 추가 예정.
- **E4 Conversation**: Comment(깊이 2). ConversationModule도 AuthModule import 패턴 사용(운영자 보호가 필요한 경우). 단 Comment 작성은 공개라 가드 불필요.

## 변경 파일

- `packages/api/src/publishing/tag.service.ts` (listUsedTags + PrismaService)
- `packages/api/src/publishing/tag.controller.ts` (신규)
- `packages/api/src/publishing/publishing.module.ts` (TagController)
- `packages/api/test/tag.e2e-spec.ts` (신규)
- `docs/tasks/blog-mvp.md` (status done)
