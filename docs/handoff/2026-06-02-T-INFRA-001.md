# Handoff — T-INFRA-001 (Prisma 스키마 정의)

- 날짜: 2026-06-02
- 태스크: T-INFRA-001 — Prisma 스키마 정의 (User/Post/Tag/PostTag/Comment + PostStatus)
- Context: INFRA (Publishing/Conversation/Auth 모델 토대)
- status: done

## 한 일

- `packages/api/prisma/schema.prisma`에 `PostStatus` enum + 5개 모델 정의 (TRD §4 일치).
- 초기 마이그레이션 생성·적용: `prisma/migrations/20260602020025_init/`.
- 로컬 마이그레이션을 위해 `docker-compose.dev.yml`의 db에 호스트 포트 노출(5433:5432 — 호스트 5432 충돌 회피).

## Acceptance 검증 결과

| # | 기준 | 결과 |
|---|---|---|
| 1 | PostStatus enum + 5모델 (TRD §4) | ✅ users/posts/tags/post_tags/comments + enum(DRAFT/PUBLISHED) |
| 2 | `prisma validate` 통과 | ✅ "schema is valid" |
| 3 | `prisma migrate dev`로 테이블 생성 | ✅ 6테이블 생성, `migrate status` up to date |
| 4 | Comment parentId 자기참조 + @@index([postId]) | ✅ `comments_parentId_fkey`(self) + `comments_postId_idx` |

검증 방법: `docker exec ... psql`로 `\dt`, `\d comments`, `\dT PostStatus` 직접 introspection.
api lint exit 0 (기존 `main.ts` 경고 1건은 무관).

## 관련 ADR

- ADR-0005 (PostStatus enum + publishedAt)
- ADR-0006 (Tag 별도 테이블 + PostTag 조인)
- ADR-0007/0013 (Comment parentId 자기참조; 깊이 제약은 서비스 계층 — T-CONV-001에서)

## 다음 태스크가 알아야 할 것

- **DATABASE_URL**: 로컬은 `postgresql://blog:blog@localhost:5433/blog?schema=public` (컨테이너 내부는 `db:5432`). `.env` 미생성 — 환경변수로 주입하거나 `packages/api/.env` 생성 필요.
- db 컨테이너: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d db` (healthy 확인 후 사용).
- Prisma 클라이언트 생성(generated/prisma)·PrismaService 와이어링은 **T-INFRA-002**에서.
- 깊이 2 제약은 스키마가 아닌 서비스 계층(T-CONV-001)에서 강제.

## 변경 파일

- `packages/api/prisma/schema.prisma`
- `packages/api/prisma/migrations/20260602020025_init/migration.sql` (신규)
- `docker-compose.dev.yml` (db 포트 노출)
- `docs/tasks/blog-mvp.md` (status done)
