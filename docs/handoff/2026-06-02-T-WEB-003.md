# Handoff — T-WEB-003 (Post 목록 + Tag 필터 페이지)

- 날짜: 2026-06-02
- 태스크: T-WEB-003
- Context: WEB
- status: done
- 의존: T-WEB-001, T-PUB-006 (done)

## 한 일

- `src/posts/usePosts.ts` — TanStack Query 훅: `GET /api/posts`(page/pageSize/tag), queryKey `['posts', params]`.
- `src/components/PostListView.tsx` — 로딩(`role=status`)/에러(`role=alert`)/빈/정상 4상태 렌더. 항목은 상세·태그 링크.
- `src/components/Pagination.tsx` — 이전/다음, 경계 disabled, `page/totalPages` 표시.
- `src/pages/PostList.tsx` — `/`, `?page=` 검색파라미터 기반 페이지 이동.
- `src/pages/TagPosts.tsx` — `/tags/:name`, tag 필터로 usePosts 호출.
- `routes.tsx`: `/`→PostList, `/tags/:name`→TagPosts (placeholder 교체).

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | / 목록 페이지네이션 | ✅ page=2 재조회 |
| 2 | /tags/:name 필터 | ✅ tag 파라미터 전달 |
| 3 | 빈/로딩/에러 상태 | ✅ 3상태 + 정상 |

게이트: lint 0, typecheck 0, test 14/14, build green.
Docker 실환경: 로그인→작성→발행 후 공개 목록 total 1, tag=nestjs total 1 확인.

## 메모

- routes 스모크의 `/` 단언을 placeholder→"최근 글" 제목으로 갱신(정당한 교체).
- 목록 항목 타이틀은 `/posts/:id` 링크 — 상세 페이지는 T-WEB-004(다음)에서 구현.
- 태그 칩은 `/tags/:tag` 링크.

## 다음 태스크가 알아야 할 것

- T-WEB-004(상세): `GET /api/posts/:id` + `react-markdown`+`rehype-sanitize`(안전한 img만), 발행일·Tag 표시. 두 패키지 설치 필요.
- 상세 페이지에서 댓글(T-WEB-005)이 이어짐.

## 변경 파일

- `packages/web/src/posts/usePosts.ts` (신규)
- `packages/web/src/components/{PostListView,Pagination}.tsx` (신규)
- `packages/web/src/pages/{PostList,TagPosts}.tsx` (+ 각 *.test.tsx, 신규)
- `packages/web/src/routes.tsx`, `src/routes.test.tsx`
- `feature_list.json`, `docs/tasks/blog-mvp.md` (status done)
