# Handoff — T-CONV-006 (CommentService.update + toDto 소프트 가림)

날짜: 2026-06-08 / 기능: comment-moderation (E18) / 컨텍스트: Conversation

## 한 일
- 댓글 수정(본인만) + 소프트 삭제 응답 가림을 TDD 로 구현했다(ADR-0027).
  - `comment.service.ts`:
    - `Actor{id,role}` 인터페이스(ADR-0018 패턴 재사용 — 수정=본인만, 삭제는 T-CONV-007).
    - `update(id, body, actor)`: `comment.userId === actor.id` 면 `body`+`editedAt=now` 갱신 → `isEdited:true`. 익명(userId null)·타인 → `ForbiddenException`(403), 없는 댓글 → `NotFoundException`(404).
    - `requireComment(id)`: 관계 포함 조회 + 404.
    - `resolveDepth(parentId)`: 단일 노드 깊이(0~2) 계산(부모 한 단계).
    - `toDto` 가림: `deletedAt != null` 이면 `body:""`·`authorName/authorAvatarUrl/displayName: null`·`isDeleted:true`. 트리·`replies`·`depth`·`userId`·`parentId` 보존(M6). 정상 노드는 기존과 동일.
  - `comment.service.spec.ts`: update 4 케이스(본인/타인403/익명403/없음404) + 소프트 가림 1 케이스(통합 테스트).

## TDD 사이클
- **RED**: update 미구현·가림 미적용 → 5 fail.
- **GREEN**: 구현 후 comment.service.spec 13 통과(update 4+가림 1+기존 8).
- **REFACTOR**: Actor 주석 명확화(수정=본인만).

## 결정한 것
- 새 ADR 불필요(ADR-0027). 소프트 가림은 **응답 레벨**(DB 원문 보존 — 감사·복구 여지). userId 는 유지하되 표시 정보(이름·아바타·displayName·body)만 가림.
- code-reviewer Suggestion(resolveDepth → update include 로 쿼리 절감)은 CommentRow 타입 복잡도가 늘어 보류(현 구현 정확·N+1 아님).

## 알려진 이슈
- `comment.service.spec` 상단 `DATABASE_URL ??= ...blog` 기본값은 기존 패턴(내가 추가 안 함). test-db.setup 이 blog_test 강제하므로 실제로는 테스트 DB 사용(verifier 확인). 신규 spec 추가 시에는 #8대로 자체 기본값 금지.

## 다음 세션이 알아야 할 것
- 다음: **T-CONV-007** (CommentService.remove 조건부 soft/hard + soft 부모 답글 차단, deps=006 done). 본인||ADMIN||글쓴이(post.authorId read-only) 권한, 직계 답글 있으면 soft(deletedAt) 없으면 hard(delete). create 시 부모 deletedAt!=null → 400. `Actor`·`requireComment`·`resolveDepth` 재사용 가능.
- 그 다음 008(컨트롤러+e2e)→T-WEB-401(웹).

## 명령 출력 증거
- comment.service.spec: 13 passed / 전체 단위: 175 passed
- api tsc 0 / lint clean / shared 무변경
- verifier: 4/4 PASS / code-reviewer: Critical·Warning 0(Suggestion 2 — 주석 반영, 쿼리절감 보류)
