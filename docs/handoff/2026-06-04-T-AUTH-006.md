# Handoff — T-AUTH-006 (shared RBAC 타입)

날짜: 2026-06-04 / 기능: multiuser-rbac (Phase 1)

## 한 일
- `packages/shared/src/dto/auth.ts`: `UserRole`('ADMIN'|'AUTHOR'|'MEMBER'), `RegisterDto` 추가.
- `packages/shared/src/dto/user.ts`(신규): `AdminUserDto`, `UpdateUserRoleDto`.
- `index.ts`에 export 추가.
- **가산적(additive)만**: `AuthUserDto`는 `{id,email}` 유지. name·role 추가는 T-AUTH-007에서 소비자(jwt.strategy/auth.service)와 함께 변경(컴파일 단위 분리).

## 검증
- 순수 타입(tdd_first:false). `pnpm --filter @blog/shared build` 통과, api·web `tsc --noEmit` 통과(기존 사용처 무영향).

## 다음
- T-AUTH-005(스키마/마이그레이션/seed=ADMIN) → T-AUTH-007(AuthUserDto name·role + JwtStrategy DB 재조회).
