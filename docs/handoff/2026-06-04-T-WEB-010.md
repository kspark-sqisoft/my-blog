# Handoff — T-WEB-010 (글 목록 카드 대표 이미지 표시)

- 날짜: 2026-06-04
- 태스크: T-WEB-010
- Context: WEB
- status: done
- 의존: T-PUB-103, T-WEB-009 (done)
- 기능: apple-redesign

## 한 일

글 목록 카드에 대표 이미지를 렌더.

- `components/PostListView.tsx` — 카드 상단에 `coverImageUrl` 이 있으면 `<img class="ab-card-cover" loading="lazy">`(상세로 가는 `<Link>` 래핑), 없으면 `<div class="ab-ph ab-card-cover">` 줄무늬 플레이스홀더.
- `index.css` — `.ab-card-cover`(width 100%, `aspect-ratio: 3/2`, `object-fit: cover`, 라운드/보더) + hover 그림자.

## TDD 사이클 확인

- 신규 stateful 로직 없음(표시 분기). 기존 `PostList.test`(역할/빈/에러/페이지네이션)이 회귀 가드 — coverImageUrl 미포함 mock에서도 플레이스홀더 경로로 정상 렌더.
- **실측 검증**(브라우저): 실 데이터에서 `img.ab-card-cover` 10개, 플레이스홀더 0개, 첫 이미지 `naturalWidth=1920`(실제 로드). 스텁(데이터 URI) 케이스로 이미지/플레이스홀더 두 분기 모두 확인.

## 결정한 것

- 이미지 없는 글은 시안의 줄무늬 플레이스홀더로 자리 유지(그리드 정렬 일관성). 대표 이미지 클릭도 상세로 이동(제목 링크와 별개 형제 링크 — 중첩 앵커 회피).

## 알려진 이슈

- 대표 이미지는 `/uploads/*` 정적 서빙(ADR-0012)에 의존. dev proxy/prod nginx 정합이 깨지면 깨진 이미지로 보일 수 있음(상세 본문 이미지와 동일 경로라 회귀 시 함께 영향).

## 다음 세션이 알아야 할 것

- 카드 비주얼은 `.ab-card*` 토큰. 대표 이미지 수동 지정 기능은 미구현(ADR-0015 / PRD 오픈 이슈).

## 명령 출력 증거

```
web typecheck: tsc -b --noEmit → 0
web unit: Test Files 13 passed (13), Tests 34 passed (34)
web lint: eslint . → 0 / build: ✓ built
live(5173): img.ab-card-cover=10, placeholder=0, firstSrc=/uploads/9ef1...jpg, naturalWidth=1920
```

commit: `a5a236d feat: 글 목록 카드에 대표 이미지(본문 첫 이미지) 표시`
