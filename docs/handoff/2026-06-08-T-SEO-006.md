# Handoff — T-SEO-006 (web index.html 기본 OG + 피드 link + og-default.png) · seo-feed 완결

날짜: 2026-06-08 / 기능: seo-feed (E17) / 컨텍스트: WEB

## 한 일
- 사이트 기본 SEO 메타를 TDD 로 추가했다(ADR-0026).
  - `packages/web/index.html`: 피드 자동발견 `<link rel="alternate" type="application/rss+xml" href="/feed.xml">` + 사이트 기본 OG(`og:site_name`·`og:type=website`·`og:title`·`og:description`·`og:image=/og-default.png`·`twitter:card=summary_large_image`·twitter:title/description/image).
  - `packages/web/public/og-default.png`: 1200x630 사이트명 카드 PNG(9.8KB). 대표 이미지 없는 글·홈·외부이미지 폴백용(T-SEO-004 OgMetaService 가 참조).
  - `packages/web/scripts/gen-og-default.mjs`: 기설치 Playwright(chromium)로 PNG 재생성(새 의존성 0). 재생성: `pnpm --filter web exec node scripts/gen-og-default.mjs`.
  - `packages/web/src/seo-static.test.ts`: index.html 메타 + PNG 매직바이트 검증.

## TDD 사이클 확인
- **RED**: seo-static 3 실패(메타·png 없음).
- **GREEN**: index.html 메타 + png 생성 → 134 통과. PNG 매직바이트(`89504e47...`)·1200x630 검증.
- **수정**: vitest globals 타입(`import {describe,it,expect} from 'vitest'`) + node API 타입(`/// <reference types="node" />`) 으로 tsc -b 통과.

## 결정한 것
- 새 ADR 불필요(ADR-0026). 글별 동적 OG 는 봇 경로(/og/posts/:slug, 절대 URL)가, index.html 기본 OG 는 홈/목록용으로 역할 분리.
- og-default.png 는 vite `public/` 표준 정적 자산 → build 시 `dist/` 복사 → favicon 과 동일 경로로 dev vite/prod nginx 서빙(verifier 가 curl 200 + image/png 왕복 확정).

## 알려진 이슈 / 후속 (백로그)
- **[후속 권장] index.html 의 `og:image`·`og:url` 절대화**: 현재 `og:image="/og-default.png"`(상대경로)라 카톡·Slack·LinkedIn 등 일부 크롤러가 **홈/목록 공유 시 카드 이미지를 못 읽을 수 있다**(글 상세는 봇 경로가 절대 URL 로 처리하므로 무관). index.html 은 정적 파일이라 빌드 시 `SITE_URL` 주입(vite `define`/치환)이 필요 — 별도 태스크로 다룰 것. (code-reviewer Warning 1)
- 사이트명/설명 문자열이 index.html·api `site-config.ts`·gen 스크립트 3곳 중복(정적 HTML 이라 통일 어려움 — 불가피).

## 다음 세션이 알아야 할 것
- **seo-feed(E17) 전체 완료**: T-SEO-001~006 done. api 4 엔드포인트(feed/sitemap/robots/og) + nginx 봇 분기 + web 기본 메타.
- 6개 기능 로드맵의 **2번째 기능 = comment-moderation**(댓글 수정·삭제) 이 다음. 추천 순서: comment-moderation → author-profile → series → bookmarks → notifications.
- seo 후속(og:image 절대화)·봇 컨테이너 스모크(T-SEO-005)는 배포 전 확인 항목.

## 명령 출력 증거
- web: lint 무오류 / typecheck(tsc -b) 무오류 / build ✓ / test **134 passed**(seo-static 3)
- og-default.png: PNG 1200x630, dist 복사 확인, curl 200 + image/png(verifier)
- verifier: 4/4 PASS / code-reviewer: Critical 0(Warning 2 — og:image 절대화[후속]·png 왕복[매직바이트로 보강])
