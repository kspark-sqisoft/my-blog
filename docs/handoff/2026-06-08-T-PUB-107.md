# Handoff — T-PUB-107 (listPublished 작성자 필터 + ?author 쿼리)

## 한 일
- `listPublished` 에 작성자 필터 추가 (author-profile, ADR-0028). 공개 발행 목록을 특정 작성자(User.id)로 좁힌다.
- 변경 파일:
  - `packages/api/src/publishing/post.service.ts` — `ListPublishedParams.authorId?` 추가, `where` 에 `...(params.authorId && { authorId })` 조건부 필터(`status:'PUBLISHED'` 와 AND). 기존 `orderBy: publishedAt desc` + `skip/take` + `$transaction([findMany,count])` 단일 트랜잭션 유지(N+1 없음).
  - `packages/api/src/publishing/dto/list-posts.query.ts` — `author?: string`(`@IsOptional @IsString`). 클래스 주석에 `&author=` 반영.
  - `packages/api/src/publishing/post.controller.ts` — `list()` 가 `authorId: query.author` 전달.
  - `packages/api/src/publishing/post.service.spec.ts` — 작성자 필터 단위테스트. **try/finally 로 타 작성자 데이터 정리 보장**(assertion 실패 시 blog_test 오염 방지).
  - `packages/api/test/post.e2e-spec.ts` — `GET /api/posts?author=` e2e(그 작성자 글만·타 작성자 제외, 없는 author → 빈 목록 200).
- 엔드포인트 신규 추가 없음(기존 `GET /api/posts` 에 쿼리 파라미터만) → CLAUDE.md AUTO-MANAGED 변경 없음.

## TDD 사이클 확인
- **RED**: 작성자 필터 단위테스트·e2e 를 먼저 작성 → `authorId` 미구현 상태에서 실패(필터 미적용으로 타 작성자 글 포함).
- **GREEN**: `ListPublishedParams.authorId` + where 조건부 + DTO/컨트롤러 전달 추가 → 단위 39/39, e2e 17/17 통과.
- **REFACTOR**: 필터를 기존 `tag`/`q` 와 동형(조건부 스프레드)으로 작성해 패턴 일치. DTO 주석 일관성(code-reviewer Suggestion #1) 반영.

## 결정한 것
- 작성자 필터는 항상 `status:'PUBLISHED'` 와 AND → author 필터로 초안/미발행 노출 불가(보안 격리, ADR-0028 §5). 새 ADR 불필요(ADR-0028 §3·§7 범위 내).
- 인덱스: 기존 `@@index([authorId, status, publishedAt])`(T-AUTH-012)가 이 쿼리(authorId+status 필터, publishedAt 정렬)를 정렬까지 커버. prisma-helper check_index 로 확인(composite). 추가 마이그레이션 불필요.

## 알려진 이슈
- 없음. (전체 회귀: api 단위 182/182, e2e 100/100 GREEN.)
- 정리한 사고: RED 단계 반복 실행으로 blog_test 에 누적된 orphan `pub107-*` 작성자/글 2건을 psql 로 청소. 재발 방지를 위해 테스트에 try/finally 정리 도입.

## 다음 세션이 알아야 할 것
- author-profile 남은 태스크(deps 충족): **T-AUTH-013**(UserController `GET /api/users/:id` 공개 프로필·email 비노출·postCount·404), **T-AUTH-014**(PATCH `/api/auth/me` bio 편집, `@MaxLength 200`), **T-WEB-402**(`/users/:id` 페이지+작성자 링크), **T-WEB-403**(`/profile` bio 편집 UI).
- T-AUTH-013 은 신규 엔드포인트 → /finish 5단계에서 CLAUDE.md AUTO-MANAGED endpoints 표 재생성 필요.

## 명령 출력 증거
- lint: `eslint --fix` 통과(무출력).
- typecheck: `tsc --noEmit -p tsconfig.json` → 에러 0(`TC_OK`).
- 단위: `pnpm --filter api test -- post.service.spec` → **39 passed**. 전체 `pnpm --filter api test` → **182 passed / 25 suites**.
- e2e: `pnpm --filter api test:e2e -- post.e2e-spec` → **17 passed**. 전체 `pnpm --filter api test:e2e` → **100 passed / 14 suites**.
- 인덱스: `check_index(Post, authorId)` → `hasIndex:true, composite: @@index([authorId, status, publishedAt])`.
- verifier: acceptance 4/4 **PASS**. code-reviewer: Critical 0 / Warning 0 / Suggestion 2(반영 1, 선택 1).

평가: Accept (12/12)
