# Handoff — T-AUTH-005 (User.role + Comment.userId 스키마)

날짜: 2026-06-04 / 기능: multiuser-rbac (Phase 1)

## 한 일
- `schema.prisma`: `UserRole` enum(ADMIN/AUTHOR/MEMBER), `User.role`(NOT NULL default MEMBER) + `User.comments` 역참조, `Comment.userId`(nullable) + `user` 관계(`onDelete: SetNull`) + `@@index([userId])`.
- 마이그레이션 `20260604010000_add_user_role_and_comment_user`: enum 생성 → role 추가(default MEMBER) → **`UPDATE users SET role='ADMIN'`**(시점상 시드 운영자만) → Comment.userId + FK(SetNull) + index.
- `seed-operator.ts`: create/update 모두 `role: 'ADMIN'` 부여(재실행 시 ADMIN 보강).
- seed-operator.spec: role=ADMIN RED→GREEN(신규/강등 후 재시드 2케이스).

## 검증
- dev `blog` + test `blog_test` 양쪽 `migrate deploy` 성공. 기존 운영자 ADMIN 백필됨.
- api 유닛 60 / e2e 35 / typecheck / lint 통과.

## 다음
- T-AUTH-007: `AuthUserDto`에 name·role 추가 + `JwtStrategy` DB 재조회(@Global Prisma 주입) + auth.service login/me 응답 확장. (소비자 동시 변경 — 컴파일 그린)
