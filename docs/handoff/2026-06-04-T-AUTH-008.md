# Handoff — T-AUTH-008 (RBAC 가드)

날짜: 2026-06-04 / 기능: multiuser-rbac (Phase 1)

## 한 일
- `roles.decorator.ts`: `@Roles(...roles)` + `ROLES_KEY`.
- `roles.guard.ts`: `RolesGuard`(Reflector) — `@Roles` 없거나 빈 배열이면 통과, user.role 불일치/없음 → `ForbiddenException(403)`.
- `optional-jwt-auth.guard.ts`: `OptionalJwtAuthGuard` — `handleRequest`가 user 없어도 throw 안 하고 undefined(익명 통과).
- `auth.module.ts`: `RolesGuard` providers + `exports: [RolesGuard, JwtStrategy, PassportModule, JwtModule]`(타 모듈 @UseGuards용).
- `roles.guard.spec.ts`: 5케이스(메타 없음/빈배열/포함/불일치 403/user 없음 403) RED→GREEN.

## 검증
- api 유닛 65(+5) / e2e 36 / typecheck / lint 통과.

## 메모
- 가드는 아직 어느 컨트롤러에도 부착 안 됨(부착·소유권은 T-PUB-105). OptionalJwtAuthGuard 동작 e2e는 T-CONV-004에서.

## 다음
- T-PUB-105: post.service 소유권(Actor) + post/admin-post/upload 컨트롤러 `@Roles` 부착.
