# Handoff — T-CONV-005 (Comment editedAt/deletedAt + shared 타입) · comment-moderation 시작

날짜: 2026-06-08 / 기능: comment-moderation (E18) / 컨텍스트: Conversation

## 한 일
- comment-moderation 의 스키마·타입 토대를 추가했다(ADR-0027).
  - `prisma/schema.prisma` `Comment`: `editedAt DateTime?`(수정 추적)·`deletedAt DateTime?`(소프트 삭제) 추가.
  - 마이그레이션 `20260608074506_comment_moderation_fields`: `ADD COLUMN` nullable 2개(DROP/NOT NULL 0). **dev `blog` + test `blog_test` 양쪽 적용**(migrate dev / migrate deploy), `prisma generate` 갱신.
  - `packages/shared/src/dto/comment.ts`: `CommentDto` 에 `editedAt: string|null`·`isEdited: boolean`·`isDeleted: boolean` + 신규 `UpdateCommentDto { body: string }`. `index.ts` 배럴에 `UpdateCommentDto` export(code-reviewer Critical 반영).
  - `comment.service.ts`: `CommentRow`·`toDto` 에 `editedAt`(ISO)·`isEdited`(editedAt!=null)·`isDeleted`(deletedAt!=null) 노출. **소프트 삭제 가림은 미구현(T-CONV-006 범위)**.
  - `CommentTree.test.tsx` mock 5개에 새 필드 추가(회귀).
- comment-moderation 기획 문서 동봉: PRD·BC(Conversation 확장)·TRD·ADR-0027·tasks·glossary(모더레이션·소프트 삭제).

## TDD/검증
- 토대(스키마·타입)라 동작 테스트는 T-CONV-006~008 이 담당. 본 태스크는 **구조 + 회귀 0** 으로 검증.
- check_migration_destructive: `destructive: false`. psql 로 blog/blog_test 양쪽 컬럼 nullable 존재 확인(verifier).
- api `tsc --noEmit` 0, 단위 170 / web `typecheck` PASS, test 134 / api·web lint clean / shared build OK.
- 함정: CommentDto 필수 필드 추가 → 생성처(api toDto 1·web mock 3) 모두 갱신, prisma client 미재생성 시 tsc 에러 → `prisma generate` 로 해소.

## 결정한 것
- 새 ADR 불필요(ADR-0027 작성됨). `editedAt`/`deletedAt` 별도 컬럼(@updatedAt 오염 회피), 소프트 가림은 응답 레벨(T-CONV-006).

## 다음 세션이 알아야 할 것
- 다음: **T-CONV-006** (CommentService.update + toDto 소프트 가림, deps=005 done). 본인만 수정+editedAt, toDto 에서 deletedAt!=null 이면 body/authorName/authorAvatarUrl/displayName 가림(M6). 익명/타인 403, 없음 404.
- 그 다음 T-CONV-007(remove 조건부)→008(컨트롤러+e2e)→T-WEB-401(웹).
- prisma client 는 `packages/api/generated/prisma`(gitignore). 스키마 변경 후 `prisma generate` 필수.

## 명령 출력 증거
- 마이그레이션: `ADD COLUMN "deletedAt"/"editedAt" TIMESTAMP(3)`(비파괴), blog+blog_test `_prisma_migrations` 기록
- api: tsc 0 / 단위 170 passed / lint clean
- web: typecheck PASS / test 134 passed / lint clean
- verifier: 4/4 PASS / code-reviewer: Critical 1(UpdateCommentDto export 누락) 수정 후 통과
