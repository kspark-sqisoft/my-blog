# Handoff — T-PUB-004 (발행 목록: 페이지네이션 + tag 필터)

- 날짜: 2026-06-02
- 태스크: T-PUB-004
- Context: PUBLISHING
- status: done
- 의존: T-PUB-002, T-PUB-003 (done)
- 관련: ADR-0010

## 한 일

- `PostService.listPublished({ page?, pageSize?, tag? })` → `Paginated<PostSummaryDto>`:
  - `status=PUBLISHED`만, `publishedAt desc` 정렬.
  - offset 페이지네이션(기본 page=1, pageSize=10) + `total`(같은 where로 count).
  - `tag` 지정 시 `postTags.some.tag.name` 필터.
  - `prisma.$transaction([findMany, count])`로 목록+합계 일관 조회.
- `toSummary()` 헬퍼: contentMarkdown 공백 정리 + 200자 제한 요약.
- 스펙 3케이스 추가(총 13).

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | 발행만 최신순(초안 제외) | ✅ |
| 2 | page/pageSize + total | ✅ |
| 3 | tag 필터 | ✅ |

게이트: lint 0, tsc 0, unit 28/28, e2e 14/14.

## 다음 태스크가 알아야 할 것

- 컨트롤러(T-PUB-006): `GET /api/posts?page=&pageSize=&tag=` → listPublished. 쿼리 파라미터는 string → number 변환 필요(ValidationPipe transform + DTO 또는 ParseIntPipe).
- 상세(T-PUB-005): 초안 비노출(공개 조회 시 404).
- 응답 형태: `Paginated<PostSummaryDto> { items, page, pageSize, total }`.

## 변경 파일

- `packages/api/src/publishing/post.service.ts` (listPublished, toSummary)
- `packages/api/src/publishing/post.service.spec.ts` (+3 케이스)
- `docs/tasks/blog-mvp.md` (status done)
