# Handoff — T-AUTH-013 (UserController GET /api/users/:id 공개 프로필)

## 한 일
- 공개 작성자 프로필 조회 엔드포인트 추가 (author-profile, ADR-0028). 누구나 `GET /api/users/:id` 로 작성자 공개 정보를 본다(이메일 비노출).
- 신규/변경 파일:
  - `packages/api/src/auth/user.service.ts`(신규) — `getPublicProfile(id)`. `$transaction([user.findUnique(select: email 제외), post.count({authorId, status:'PUBLISHED'})])` 단일 왕복(N+1 없음). user 없으면 `NotFoundException`(404). `createdAt.toISOString()` → `AuthorProfileDto`.
  - `packages/api/src/auth/user.controller.ts`(신규) — `@Controller('users')` 공개 `GET :id`(가드 없음).
  - `packages/api/src/auth/auth.module.ts` — `UserController`/`UserService` 등록.
  - `packages/api/src/auth/user.service.spec.ts`(신규) — 단위 3(매핑·email 비노출·select 검증·postCount PUBLISHED만 1회 / 404 / MEMBER postCount 0).
  - `packages/api/test/user.e2e-spec.ts`(신규) — e2e 4(404 / 작성자 postCount=2 초안제외·email 비노출 / MEMBER 0 / 아바타 업로드→PATCH 영속화→프로필 avatarUrl /uploads 200 image 왕복).
- `AuthorProfileDto` 는 T-AUTH-012 에서 이미 shared 에 정의됨(이번 변경 없음).
- 신규 엔드포인트 → `CLAUDE.md` AUTO-MANAGED endpoints 표에 `GET /api/users/:id`(공개) 추가.

## TDD 사이클 확인
- **RED**: user.service.spec / user.e2e-spec 먼저 작성 → `Cannot find module './user.service'` 로 실패.
- **GREEN**: UserService + UserController + 모듈 등록 → 단위 3/3, e2e 4/4 통과. (e2e 아바타 케이스: 업로드는 파일 저장+URL 반환만 하므로 PATCH /api/auth/me 로 영속화 후 조회하도록 테스트 보정.)
- **REFACTOR**: lint(`no-unsafe-member-access`)로 걸린 `mock.calls[0][0]` 접근을 튜플 구조분해(`const [firstArg] = ... as [{select}]`)로 정리.

## 결정한 것
- 프로필 조회 + postCount 를 `$transaction([findUnique, count])` 단일 왕복으로(N+1 없음, ADR-0028 결정 9). 없는 id 도 count 1회 실행되지만 단순성 우선(code-reviewer Suggestion — 최적화는 선택).
- email 은 `select` 에서 배제 + DTO 에 필드 없음(이중 차단). 공개 프로필 cuid 노출은 ADR-0028 결정 1이 명시 수용(공개 정보만, 핸들은 후속 ADR). 새 ADR 불필요.
- postCount 쿼리는 기존 `@@index([authorId, status, publishedAt])` 가 커버(prisma-helper check_index 확인). scan_pii_logging 0건.

## 알려진 이슈
- 없음. 전체 회귀: api 단위 185/185, e2e 104/104 GREEN.
- code-reviewer Suggestion(선택, 미반영): 404 e2e id 가 cuid 형식 아님(향후 cuid 검증 파이프 추가 시 400 될 수 있음). 현재 스펙 영향 없음.

## 다음 세션이 알아야 할 것
- author-profile 남은 태스크: **T-AUTH-014**(PATCH `/api/auth/me` bio 편집, UpdateProfileDto `@MaxLength 200`) → 그다음 **T-WEB-402**(`/users/:id` 페이지+작성자 링크, useAuthorProfile/usePosts({author}) 훅), **T-WEB-403**(`/profile` bio 편집 UI).
- T-WEB-402 는 이번 `GET /api/users/:id` + T-PUB-107 `?author` 를 소비한다.

## 명령 출력 증거
- lint: 통과(초기 `no-unsafe-member-access` 1건 → 수정 후 0).
- typecheck: `tsc --noEmit -p tsconfig.json` → 에러 0(`TC_OK`).
- 단위: `pnpm --filter api test -- user.service.spec` → **3 passed**. 전체 → **185 passed / 26 suites**.
- e2e: `pnpm --filter api test:e2e -- user.e2e-spec` → **4 passed**. 전체 → **104 passed / 15 suites**.
- 인덱스: `check_index(Post, authorId)` → composite `@@index([authorId, status, publishedAt])`. PII: `scan_pii_logging` → 0건.
- verifier: acceptance 4/4 **PASS**. code-reviewer: Critical 0 / Warning 0 / Suggestion 3(전부 선택).

평가: Accept (12/12)
