# Handoff — T-WEB-501 (시리즈 상세 페이지 /series/:slug)

## 한 일
- 공개 시리즈 상세 페이지 추가 (series, ADR-0029). `GET /api/series/:idOrSlug`(T-SER-004) 소비.
- 신규/변경 파일:
  - `packages/web/src/pages/SeriesDetail.tsx`(신규) — `useSeries(slug)` → 헤더(시리즈 제목·설명·작성자 `/users/:authorId` 링크·N편) + `PostListView items` 모드(emptyText "아직 발행된 글이 없습니다."). 로딩/에러(404) 상태.
  - `packages/web/src/series/useSeries.ts`(신규) — `GET /api/series/:idOrSlug` useQuery(encodeURIComponent, enabled).
  - `packages/web/src/components/PostListView.tsx` — **items 모드 추가**(`query?` optional + `items?: PostSummaryDto[]`). query 모드 기존 동작 보존(회귀 0). 시리즈 상세처럼 인라인 배열(SeriesPostItemDto[])을 직접 렌더하도록 재사용.
  - `packages/web/src/routes.tsx` — `/series/:slug` 등록(PublicLayout 하위).
  - `packages/web/src/pages/SeriesDetail.test.tsx`(신규, 5건).

## TDD 사이클 확인
- **RED**: SeriesDetail.test 5건 → SeriesDetail import 실패.
- **GREEN**: useSeries → PostListView items 모드 → SeriesDetail → 라우트 → 5건 통과(초기 "1편"(postCount) ↔ 글 제목 "1편" 텍스트 충돌은 글 제목을 "첫 번째 글"로 바꿔 해소).
- **REFACTOR**: PostListView query/items 분기 정리(query truthy 시 로딩·에러·data, 아니면 items). 전체 web 154 GREEN.

## 결정한 것
- `SeriesDetailDto.posts`는 인라인 배열(쿼리 아님)이라 PostListView를 **items 모드**로 확장해 재사용(tasks "PostListView 재사용" 충족). `query?`/`items?` optional 전환 — 기존 4개 호출처(PostList/TagPosts/AuthorProfile/홈) query 모드 무변경. 새 ADR 불필요.
- 작성자 링크는 헤더(시리즈 작성자)와 카드(글 작성자) 둘 다 `/users/:authorId`(접근가능 이름=이름). 404 등 모든 에러는 동일 에러 UI(상태코드 분기 없음 — 현 설계).

## 알려진 이슈
- 없음. web 154/154 GREEN, typecheck·lint 클린.
- code-reviewer Warning(선택, 미반영): PostListView에 query·items 둘 다 없으면 무음 빈 렌더 — 판별 유니온(`{query}|{items}`)으로 컴파일 타임 강제 가능. 현 호출처 4곳 모두 정확히 한 모드만 사용 → 실버그 없음. 후속 개선 여지.

## 다음 세션이 알아야 할 것
- 다음 후보: **T-WEB-502**(글 상세 시리즈 네비 UI — `PostDetailDto.series`(T-SER-005) 소비, 이전/다음·N/M편 + 시리즈 상세 링크, deps T-SER-005 done). 이어서 T-WEB-503(시리즈 목록 `/series`, `GET /api/series` 소비), T-WEB-504(작성자 관리 UI).
- `PostListView`는 이제 `items` 모드를 지원(T-WEB-503 목록은 `usePosts` 같은 query 모드 또는 useSeriesList).

## 명령 출력 증거
- 단위: SeriesDetail 5건 GREEN. 전체 `pnpm --filter web test` → **Test Files 33 passed / Tests 154 passed**.
- typecheck: `tsc -b --noEmit` 통과. lint: `eslint .` 통과.
- verifier: acceptance 7단언 **PASS**. code-reviewer: Critical 0 / Warning 1(판별 유니온, 선택) / Suggestion(404 식별·queryKey 양호).

평가: Accept (12/12)
