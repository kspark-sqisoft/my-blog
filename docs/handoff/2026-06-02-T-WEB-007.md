# Handoff — T-WEB-007 (Post 작성·수정 에디터 + 이미지 업로드)

- 날짜: 2026-06-02
- 태스크: T-WEB-007
- Context: WEB
- status: done
- 의존: T-WEB-006, T-PUB-102, T-PUB-009 (done)

## 한 일

- `src/admin/usePostEditor.ts` — `useAdminPost`(GET /admin/posts/:id), `useCreatePost`(POST /posts),
  `useUpdatePost`(PATCH /posts/:id), `useUploadImage`(POST /uploads, multipart).
- `src/pages/admin/PostEditor.tsx` — 제목/본문(마크다운)/태그 폼, 이미지 업로드 → 본문에 `![name](url)` 삽입,
  Tag 6개 이상 클라 검증(role=alert), 저장 후 /admin 이동. 수정 모드는 admin 단건에서 초기값 로드.
- `routes.tsx`: `/admin/posts/new`·`/admin/posts/:id/edit` → PostEditor. 미사용 `Placeholder` 제거.

## Acceptance 검증 (3/3 ✅)

| # | 기준 | 결과 |
|---|---|---|
| 1 | 제목/본문/Tag create·update 연동 | ✅ POST/PATCH, 수정 시 admin 단건 로드 |
| 2 | 이미지 업로드 → URL 본문 삽입 | ✅ FormData multipart |
| 3 | Tag 6개 이상 클라 검증 차단 | ✅ |

게이트: lint 0, typecheck 0, test 32/32, build green.

## 메모

- 수정 모드 폼 초기화는 effect 대신 **렌더 중 조건부 setState(React 권장 패턴)** 로 구현 — react-hooks 린트 통과.
- 모든 자리표시자 라우트가 실제 페이지로 교체됨(Placeholder 삭제).

## 다음 태스크 (마지막)

- T-WEB-008: Playwright E2E. 운영자 로그인→작성→업로드→발행 / 독자 목록→상세→깊이2 댓글 / 미발행 비노출.
  - 실행에는 web(5173)+api 기동 필요. Playwright 설정/스펙 추가.

## 변경 파일

- `packages/web/src/admin/usePostEditor.ts` (신규)
- `packages/web/src/pages/admin/PostEditor.tsx` (+ test, 신규)
- `packages/web/src/routes.tsx`
- 삭제: `packages/web/src/components/Placeholder.tsx`
- `feature_list.json`, `docs/tasks/blog-mvp.md`
