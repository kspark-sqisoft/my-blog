{
  "name": "@blog/shared",
  "version": "1.0.0",
  "description": "프론트·백엔드 공유 타입 계약 (순수 TS, ADR-0004)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no runtime tests (순수 타입 — typecheck로 검증)\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
